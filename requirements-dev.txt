# Dev / test dependencies for the pipeline engine's OWN test suite (tests/).
# Runtime deps are declared in pyproject.toml [project.dependencies]; this file
# adds only what running the tests needs on top of an editable install.
#   python -m venv .venv && .venv/bin/pip install -e . -r requirements-dev.txt
#   .venv/bin/pytest
pytest>=8.0
