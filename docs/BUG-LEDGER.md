# Bug Ledger

Every bug found gets a row the moment it is found — even if you fix it in the
same commit, and even if you don't fix it at all. Fixed bugs move to the Fixed
table with proof (command + output, or test name). See `CLAUDE.md` §2.

## Open

| ID | Found | Where | Symptom | Notes |
|---|---|---|---|---|
| B-132 | 2026-09-14 | `pipeline/scanner/onpage_audit.py` | **DataForSEO Site Health cannot report site-wide duplicate titles or descriptions.** Verified live 2026-09-14: the per-page `checks` object returned no `duplicate_title`, `duplicate_description`, `duplicate_content` or `broken_links` keys, although the docs list them; our free crawl found "Departments & Clinics \| Orienda Hospital" on 6 pages on the same site. So Crawl Issues on DataForSEO shows passed status/orphan checks but never duplicates. | Add the `on_page/duplicate_tags` call after the crawl (documented as not charged beyond the task) and map it to `dfs.op.duplicate_title` / `duplicate_description`. Until then, duplicates come only from Our tools. |
| B-120 | 2026-09-14 | `web/app/api/scan/route.ts` (`spentTodayUsd`), `web/app/ScannerApp.tsx` (`run`), `pipeline/scanner/server.py` (`emit`) | **HIGH. The daily budget undercounts real DataForSEO spend.** It sums `scans.cost` rows that only the browser writes after a result arrives. A scan abandoned mid-run (reload; the scanner swallows BrokenPipe and keeps billing), one that ends in `{error}` after paid tools ran, or concurrent scans from two tabs are admitted against a stale total. Verified hermetically by the review: 4 paid calls billed after the pipe broke, none recorded. | Mitigated, not fixed: generic buttons are free-only again and `run()` allows one scan at a time. Real fix: record spend server-side as each paid tool finishes (or reserve `estimatedCost` in `/api/scan` for the life of the request). |
| B-121 | 2026-09-14 | `pipeline/scanner/plan.py:73`, `web/app/api/plan/route.ts:242` | **The ratchet marks a finding RESOLVED when its tool simply did not run** (paused, no credentials, budget-stripped, provider error, or a section scan without it). `build_plan` with `unavailable.backlinks` in the current scan and `dfs.broken_backlinks` in the previous one returns RESOLVED=1. The UI renders neither `resolved` nor `counts`, so users see the item silently missing from the worklist. | Resolve only findings whose tool ran in the current scan (scan_tools / tool events are already stored per scan); keep the rest open as not re-measured. |
| B-122 | 2026-09-14 | `web/app/ScannerApp.tsx` (`runApply`), `web/app/api/remediate/apply/route.ts` | **Apply refusals are lost.** `runApply` keeps the hand-rolled NDJSON loop B-108 replaced in `run()`; the route answers 200 when the backend is unreachable; every refusal becomes "apply produced no result", and the Auto-Fix screen never renders `apply.error` (it shows a hardcoded success banner). Pre-existing. | Reuse `readScanStream`-style reading for apply; 503 in the route; render the error. |
| B-123 | 2026-09-14 | `pipeline/audit/providers.py:95` | **The CLI MEASURE rail is still hard-frozen** by a third d64662d latch (`PAID_API_SPEND_FROZEN` for any api.dataforseo.com / api.brightdata.com URL) that ignores `availability()` and `DATAFORSEO_PAUSE_SPEND`. `wf-site-health --with-dataforseo` reports `failed: PAID_API_SPEND_FROZEN`, and `wf-site-plan` then counts earlier paid findings as RESOLVED. The web scanner spends under the same env. | Deliberately left: the CLI rail was out of scope for this revamp. If lifted, gate `dataforseo_findings` on `dataforseo.availability()` so the kill switch covers it. |
| B-124 | 2026-09-14 | `pipeline/outreach/qualify.py:30` | **`wf-outreach --qualify` now makes one paid call per candidate domain** when DataForSEO credentials are exported into the process and the kill switch is off, with no budget, cap or printed cost. (Credentials only in the repo-root `.env` do not trigger it: `wf-outreach` never loads `.env`.) The docstring and `docs/MODULES.md` said "pay-frozen"; both corrected in this commit. | Add a `--max-domains` / confirmation and print cost per domain. |
| B-125 | 2026-09-14 | `pipeline/scanner/dataforseo.py` (`call` retry) | **PLAUSIBLE, unverified live: a retried POST may be billed twice.** A bare `TimeoutError` / `RemoteDisconnected` after the body was delivered is retried; `cost_of` counts only the final response. DataForSEO's FAQ says it charges only when a result is delivered, which would make this moot. | Confirm with one deliberately short-timeout live call, or stop retrying post-send timeouts. |
| B-126 | 2026-09-14 | `web/app/ScannerApp.tsx` (`run`) | **A scan of another domain is still filed under the open project and merged into its report** (was B-109, marked fixed too early). The domain-owner lookup only runs when no project is open, and on every fresh load the first project is opened automatically, so the change almost never applies. | File by the scanned domain's owner (or refuse a URL whose domain is not the open project's), and merge into the open report only when they match. |
| B-114 | 2026-09-14 | `web/app/ReaiDashboard.tsx` (`TAB_ROUTES`, legacy tabs) | **Two screens per name, one fabricated.** `/keyword-gap`, `/keyword-magic-tool`, `/backlink-audit`, `/organic-research` (competitor map), `/backlink-analytics` (`refDelta "+4 ▲"`) render invented data while the sidebar entries of the same name open the report views. Report views have no URL. | Tools-revamp Phase 2: delete the legacy tabs, route those URLs to the views, give every view `/tools/<id>`. |
| B-115 | 2026-09-14 | `web/lib/reportViews.ts` (`site-crawl`, `backlink-audit`) | **Screens promise rows no scan produces.** `dfs.duplicate_*`, `click_depth`, `orphan_page`, `redirect`, `canonical_chain`, `image_alt_missing`, `broken_links`, `broken_page` come only from `dataforseo.site_audit`, which nothing outside tests calls. | Tools-revamp Phase 3: drop the dead codes (verify `site_audit` has no caller first). |
| B-116 | 2026-09-14 | `web/app/ReaiDashboard.tsx` (`backlinkGapData = []`) | **Backlink Gap has no data source.** Hardcoded empty list, no scanner tool, badge reads "0 Link Opportunities". | Tools-revamp Phase 3: new `backlink_gap` tool on DataForSEO `/v3/backlinks/domain_intersection/live`. |
| B-117 | 2026-09-14 | `.env.example`, no `web/.env.example` | **Setup documents 7 of ~40 variables.** `SCAN_TOKEN` (every scan refused without it), `PAGESPEED_API_KEY`, `DATAFORSEO_PAUSE_SPEND`, `DFS_LOCATION_CODE`, Supabase and Google OAuth keys are all missing. | Tools-revamp Phase 4. |
| B-062 | 2026-09-12 | `pipeline/gates/e2e_check.py` + `pyproject.toml` | **The E2E gate (#20) is implemented but NOT wired — and untested.** `wf-e2e-check` exists and is a valid gate, but it is not registered in `pipeline/lib/baseline.py` `NEVER_BASELINEABLE` and not invoked by `.github/workflows/quality-gate.reusable.yml`, so it gates nothing today. It also ships UNVERIFIED (no test, `pytest` not run this session). This is the B-007 "implemented is not wired" shape, recorded ON PURPOSE: wiring an untested gate into the path that blocks every client's production PR is the exact never-green risk we warn about. | Not a defect to hot-fix — a deliberate staging point. Sequence: write `tests/test_e2e_check.py`, get a green run, add it to `NEVER_BASELINEABLE`, then add the step to `quality-gate.reusable.yml` and re-count `gate-reference.md` to 8+10+2=20. Until then, run it by hand: `wf-e2e-check --project <repo>`. |
| B-061 | 2026-09-12 | `pipeline/audit/measure.py` (`_visible_text_ratio`, `health.csr_empty_shell`) + `pipeline/scanner/extra_checks.py` | **CSR detection is a raw-HTML heuristic, not a real render, and can misclassify.** The check flags a page as a client-rendered shell when raw HTML carries < 100 readable words AND text/markup ratio < 0.05. It cannot see anything a headless browser would: a heavily-cached or minified SSG page with legitimately terse first-load HTML could trip it, and a CSR page that server-sends a large static skeleton (lots of markup, little copy) could slip under the ratio and pass. The SOP describes a "< 20% of on-screen text" check; this approximates that without ever rendering, so "on-screen text" is never actually measured. Ported verbatim from the scanner so both rails agree, which also means both share the blind spot. | Accept as a cheap first-pass signal, not a verdict. A true fix needs a headless render (Playwright/Puppeteer) to diff raw vs hydrated text — out of scope for a stdlib-only engine. Also UNVERIFIED beyond an inline direction smoke-check (flags a `__next` shell, passes a 400-word SSR page). If false positives show up on real clients, gate it behind a higher word floor before trusting it in the ratchet. |
| B-059 | 2026-09-12 | `web/lib/executiveReport.ts` (`executiveMarkdown`) | **The client-facing Executive Report is written for an engineer, and breaks the writing standard we enforce on clients.** Generated from a real free scan of example.com it contains: **(a)** the headline **"Technical Health Score: 0 / 100"** - B-055's penalty score reaching a paid deliverable, for a site where 14 of 51 checks pass and Lighthouse Performance is 100/100, which is the worst possible first line of a client document; **(b)** findings titled `[critical] Title length — seo`, pairing a raw row label with the internal tool key; **(c)** **5 HTML tags shown to the client** (`Rewrite the <title> to 30-60 characters`, `Add a <link rel="canonical">`), when the operator's explicit requirement is that clients do not read code; **(d)** **10 em dashes**, in a document this product ships to clients, while `em_dash_check` blocks a client's PR for a single one and accepts no baseline (B-008). We break our own rule in our own deliverable. **(e)** it does not use the `plain`/`impact` client register added the same day, so the plain-English copy exists and nothing reads it. Reproduce: build the report from any scan and count - `(md.match(/—/g)||[]).length` -> 10, `(md.match(/<[a-z][a-z0-9]*[ >\/]/gi)||[]).length` -> 5. | Not fixed. Depends on two decisions already open: B-055 (which number is 'health') and how much of the 24-code table gets a `plain` entry - 8 of 24 today, so a rewrite now would fall back to operator language for the other 16 and read inconsistently. Sequence: finish the plain register, settle the score, then rewrite this to consume both. |
| B-058 | 2026-09-12 | `web/lib/server-security.ts:87-97` (`authenticateRequest`) | **The API-side auth fallback is not hostname-scoped either.** When `process.env.NODE_ENV !== "production"`, any unauthenticated request to any API route is granted the user `00000000-0000-0000-0000-000000000001` / `local-dev@example.com`. This is B-054's shape one layer deeper and materially worse: B-054 renders a dashboard, this one **authorises API calls** - scans that spend money, client reads, remediation applies. A non-production build reachable on any network serves those to anyone. Found while driving `/api/content/generate` locally, which the fallback is what let me do. | **Not fixed - same decision as B-054 and it should be taken with it.** Removing the fallback breaks local testing without a Supabase session, which is how the whole web app is currently exercised on this machine; the honest fix is probably a hostname check plus an explicit opt-in env var, not deletion. Nothing is deployed, which is why this is recorded rather than hot-fixed. |
| B-054 | 2026-09-12 | `web/app/auth.tsx:88-93` (`AuthGate`) | **The auth bypass is not scoped to localhost.** `isLocal` is true when the hostname is localhost/127.0.0.1 **or** `process.env.NODE_ENV !== "production"`. The third clause has no hostname condition, so any build that is not a production build signs the visitor in automatically as `DEV_SESSION` (`local-dev@example.com`, user id `00000000-0000-0000-0000-000000000001`) after a 600ms timer - on any host. A staging or preview deploy built in development mode would serve the whole dashboard, every client row and every connected Google property to anyone with the URL, with no login. Found when the operator asked why the dashboard showed data without signing in. Reproduce: `sed -n '88,93p' web/app/auth.tsx`. | **Not fixed - needs an operator decision, and it is a security change.** The fix is to drop the `NODE_ENV` clause so only a localhost hostname bypasses, but that also changes the local workflow for anyone running a non-production build on a LAN address, so it should not be changed silently. Nothing is deployed yet, which is why this is recorded rather than hot-fixed. |
| B-052 | 2026-09-12 | `web/components/dashboard/Overview.tsx` | **343 lines rendered by nothing.** Exported from `components/dashboard/index.ts` and imported by no file, so the live Overview is still the inline block in `ReaiDashboard.tsx`. B-007's shape - a finished module called by nothing - one release on from the extraction that created it. Reproduce: `grep -rE 'import \{[^}]*\bOverview\b' web/app web/components` -> no hits; `grep -rn '<Overview[ />]' web` -> no hits. Checked every sibling: it is the only dead one, the other nine all render. Found while wiring the journey bar, which Overview also calls - so the dead file was passing `hasClient={true}` to a component that no longer accepts it. | Not fixed. Kept in step with the journey change rather than left to rot, but deleting 343 lines or wiring a second Overview are both product calls, not cleanup. Needs a decision either way. |
| B-050 | 2026-09-12 | `pyproject.toml:92` (pytest `addopts`) | **36 DataForSEO tests are excluded from every default run.** `addopts = "-ra --strict-markers --ignore=tests/test_dataforseo_retry.py -k 'not dataforseo'"`, so `python -m pytest -q` reports **1005 passed, 32 deselected** and a green suite says nothing about the largest paid provider in the product. Reproduce: `python -m pytest -q` -> note the deselection; `python -m pytest tests/ -q -k dataforseo --override-ini="addopts=-ra --strict-markers"` -> **36 passed** in under a second. Found while reading the suite output during the thermo-nuclear pass on the pass-row work. | Not fixed. They pass and they are fast, so speed is not the reason and the exclusion looks deliberate but is undocumented - possibly live-network guarding that no longer applies. Flipping what CI runs is not a side effect to slip into an unrelated commit; it needs the operator who added the flag. Recorded in CHANGELOG under Known. |
| B-048 | 2026-09-12 | `web/lib/reportViews.ts` | **Web brand mentions reach no sectioned screen.** `pipeline/scanner/mentions.py` stamps a `mention.*` family on every run of the paid Reputation tool, and no report view claims it: the rows show only on the combined audit list. Found by the new reverse-coverage test, which forced the question. Reproduce: `grep -n 'make_row("mention")' pipeline/scanner/mentions.py` -> one hit; `grep -n 'mention\.' web/lib/reportViews.ts` -> none. | Not fixed - a screen needs a nav home and the SEO tree has no Reputation group, which is a product decision, not a patch. Recorded loudly instead: `web/tests/reportViews.test.mjs` lists `mention` in `UNSECTIONED` with this reason, so the gap is a decision on the record and a new family cannot be added silently. |
| B-043 | 2026-09-11 | `web/app/ReaiDashboard.tsx:6718` and `:6942` | **The Auto-Fix worklist invents a target file path, and the one it invents is healthcare-specific.** A finding whose code contains `schema` is rendered with `targetFile` `src/components/MedicalBusinessSchema.tsx`, for every client whatever their industry, and the other branches guess `components/SEOHead.tsx` and `src/app/layout.tsx` the same way. Nothing reads the client's checkout, so the path is a plausible invention displayed as the file the agent will edit. Found while removing the same `MedicalBusiness` claim from the Measure screen's pillar cards (this commit); the pillar bullet is fixed, this is not. Reproduce: `grep -n MedicalBusinessSchema web/app/ReaiDashboard.tsx` → two hits, neither behind a condition on the client. | Not fixed — out of the scope this commit was authorised for, and the honest fix is not a rename. The real target file is whatever `wf-site-plan` wrote into the cycle's `worklist.json`; when a work item carries no path the screen should render an em dash, not a guess. Guarded against re-entry on the Measure screen only, by `web/tests/measure.test.mjs` ("no healthcare schema is claimed for every client"). |
| B-042 | 2026-08-24 | `pipeline/audit/remediate.py` — `--recommend`'s argparse help text vs. the `HUMAN_WORKLIST_PATH`-style constant near `remediate.py:97` | **The brief output path is documented two different ways in the same file.** `--recommend`'s own CLI help text says briefs land at `docs/audit/human-worklist.md`; the constant actually used near `remediate.py:97` (and `read_briefs()`/`render_briefs()`) is `docs/human-worklist.md` — no `audit/` segment. Surfaced during a thermo-nuclear review of a `docs/SYSTEM-PLAN.md` §1e/§1f/§1g addition that reuses `read_briefs()`/`render_briefs()` as a source; not caused by that change, pre-existing. | Not fixed. One-character-class fix (align the help string to the real constant, or vice versa) once someone is in this file for another reason — low severity, but worth fixing before §1e's report generator hardcodes either path. |
| B-035 | 2026-08-11 | `lee-series-web/docs/client-config.yml:83` (client data, not engine code) | **Both legal gates are switched off on a skincare client, by a declaration whose stated reason only justifies deleting one default rule.** lee declares `forbidden_phrases: []`, and the comment under it reads *"The default no-dollar-amounts rule does NOT apply here: leeserie.com is a storefront and prices are the product."* That is a sound argument for dropping `\$[0-9]`. It is not an argument that the client has **no** banned phrases — but an empty list is the all-or-nothing declaration, so one rule's removal took the whole ruleset with it. Verified, both gates, against the real checkout on v3.1.0 code: `wf-rules-selftest ~/clients/lee-series-web` → `[SKIP] no ruleset to self-test … a recorded human decision, not a ruleset that passed.` exit 0; `wf-forbidden-sweep source ~/clients/lee-series-web` → `[SKIP] source mode not run … This is a recorded human decision, not a clean sweep.` **The engine is behaving exactly as designed** — `ruleset_declared_empty`'s three-state contract is working, and it says so in both messages rather than reporting green. The gap is the client's, and it is on the one gate CLAUDE.md marks never-baselineable *for legal exposure*. What makes it worth a row rather than a shrug: lee sells cosmetics, including pregnancy and stretch-mark products, which is the category where the cosmetic/drug claim line is regulated. The live copy already sits on it — `lib/seo-constants.ts:11` *"built to help you heal, love, and celebrate your skin"*, `lib/page-meta.ts:18` the same *"heal"*, and `lib/learn-guides.ts:27` *"cleanse, **treat**, and seal in one routine"*. `heal` and `treat` are the two verbs that distinguish a cosmetic from a drug claim. | **Not fixed, and deliberately not fixed by me.** Writing a legal ruleset for a paying client's live site is derivation from something I do not have: the client's regulatory position, their market (Cambodia/KH, not FDA jurisdiction by default), and whatever counsel they have taken. Inventing one would be the exact failure `claim_provenance_check` exists to prevent, applied to the ruleset instead of the copy. **This needs the operator to ask the client**, then either declare real rules or re-affirm the empty list on its own merits rather than on the price-rule argument. Two smaller things the engine could do, both open questions rather than recommendations: (1) `bootstrap_config` could refuse to write `forbidden_phrases: []` in the same edit that deletes the default rule, since the two decisions got conflated here; (2) the skip message could name the client's own sector when one is declared. Related to **B-008** (`em_dash_check` accepts no baseline) — both are "the legal-adjacent gates have no partial state", and both want a human decision, not a workaround. |
| B-031 | 2026-08-10 | `pipeline/lib/common.py:766` (message) + `_default_ssr_model` at `:152` | **The static-export warning cites an assumed default as if it were evidence, and prints a self-contradiction.** The v3 §6 warning renders as `NOT a static export (ssr_model=static-export)`. Both halves come from different sources: `static_export` is derived from the repo (absence of `output: 'export'` in a next.config) and is **correct**; `ssr_model` is **not declared by this client at all**, so `common.py:594` falls back to `_default_ssr_model("next")`, which hardcodes `"static-export"` for every Next repo. The parenthetical that is supposed to substantiate the warning is therefore an unverified assumption that directly negates the sentence containing it. Found on the first onboard of `lee-wave/lee-series-web`. Reproduce: `./run.sh wf-onboard lee-wave/lee-series-web www.leeserie.com --clients-dir /clients --tier 1` → `[WARN] NOT a static export (ssr_model=static-export) — orphan_check and parity_check ... report GREEN`. Confirmed against the repo: `grep -n output ~/clients/lee-series-web/next.config.ts` returns nothing (no static export, matching `static_export=False`), and `grep -n ssr_model docs/client-config.yml` returns nothing (never declared). The same run's profile block prints the two side by side as `ssr: static-export` on line 4 and `static export: NO` on line 5. | Not fixed — no wrong verdict, the warning still fires on the right condition, so this is a credibility defect rather than a logic one. It matters because of what the warning guards: v3 §6's silent-green, where `orphan_check` and `parity_check` scan nothing and report clean. An operator who reads a warning that contradicts itself is being invited to dismiss the one message standing between them and a gate suite that passes over zero files. Minimum fix is to stop printing an undeclared default as evidence: either omit the parenthetical when `ssr_model` was defaulted rather than declared, or track provenance and render `ssr_model=static-export (assumed, not declared)`. The broader question is whether `_default_ssr_model` should map `next` to `static-export` at all — `create-next-app` does not produce a static export by default, so the default is wrong in the common case, the same allowlist-shaped error as B-027. |
| B-026 | 2026-08-10 | `pipeline/audit/remediate.py` `run_agent` (`--allowedTools`) | **`--allowedTools` does not actually restrict the writer, so the tool allow-list is a hint rather than a boundary.** `ALLOWED_TOOLS = "Read Edit Write Grep Glob"` deliberately excludes Bash — the module docstring says a remediation run "has no business running commands" and names prompt injection carried in the client's own page copy as the reason. The first live `--recommend` run on `www.leeserie.com` (2026-08-10) shows the writer calling **Bash** anyway, in the streamed transcript: `{"name":"Bash","input":{"command":"python3 - <<'EOF' ..."}}` while reading a sibling product page. So the narrowest-blast-radius claim in the docstring is not enforced by the flag it cites. | Not fixed — needs a look at whether `--permission-mode acceptEdits` overrides the allow-list, or whether `--disallowedTools` is the flag that actually binds. **Recommend mode does not rely on it:** the clean-tree assertion in `remediate()` measures the working tree before and after and refuses any modification, which is a measurement rather than a permission, and it held on that same live run. `RECOMMEND_TOOLS = "Read Grep Glob"` was added as the narrowing it can be, not as the boundary it is not. The fix path DOES still lean on the flag, and `tier_verdict` on every touched file is what catches it there. Worth resolving before anyone widens `ALLOWED_TOOLS`. |
| B-028 | 2026-08-10 | `pipeline/audit/preflight.py:40` + six other gate CLIs | **Half the commands take `--project`, half take a positional path, and `wf-preflight --help` is broken.** `wf-site-health`, `wf-site-plan`, `wf-site-remediate` and `wf-render-snapshot` use `--project`; `wf-preflight`, `wf-rules-selftest`, `wf-capsule-check`, `wf-noncommodity-check`, `wf-forbidden-sweep`, `wf-audit-built` and `wf-client-docs-check` take it positionally. `preflight.py` reads `sys.argv[1]` directly with no argparse, so `wf-preflight --help` prints `[STOP] --help/docs/client-config.yml missing. Run bootstrap-config.py first.` and `wf-preflight --project <dir>` reports the config missing when it is present. | Not fixed. Onboarding is unaffected — `onboard.py:308` calls `wf-preflight <path>` correctly — so this only bites a human running a stage by hand, which is exactly what an operator does when a run goes wrong. Minimum fix is argparse in `preflight.py` accepting the path positionally AND as `--project`, so both spellings work and `--help` works at all; the rest can converge at leisure. Found by running the commands rather than reading them. |
| B-017 | 2026-08-07 | `.github/workflows/quality-gate.reusable.yml` (`render_url` / `snapshot` steps) + `.github/examples/quality-gate.yml` | **The CI render-source path has never run against a live Cloudflare preview.** `wf-render-snapshot` itself is tested and was driven end to end against a local HTTP server: it produced `<route>/index.html` + `sitemap.xml`, and `em_dash_check`, `check_headings` and `parity_check` all then ran and gave real verdicts on a client with no static export. What is **unverified** is the workflow wiring: whether `steps.snapshot` fires when `build-site` fails, whether `steps.tree.outputs.ready` gates the fifteen OUT steps correctly in a real run, and whether a caller can actually thread `needs.notify-preview.outputs.preview_url` into it. Asserted only as workflow TEXT by `tests/test_ratchet_wiring.py`. | Not a defect, a gap in evidence — recorded because the phase 6 provider paths are in the CHANGELOG as unverified for exactly this reason. **Also a new coupling worth a human decision:** `preview.reusable.yml` is monitoring-only today and "never blocks", so pointing a blocking gate at it makes a Cloudflare outage a blocked PR. The gate exits **4** (*cannot judge*) rather than 0 when the crawl fails, so it fails safe — but it should be watched on one real PR before any client relies on it. |

| B-019 | 2026-08-07 | `pipeline/audit/providers.py` `parse_serp` + `SERP_REACHABLE_MAX` | **`serp.page_two` can never fire, so the provider only ever emits one of its two codes.** The bands assume `organic[]` carries ranks 1–30. It does not: Google returns one page of organic results, and the count is *below* ten because videos, images and shopping blocks take the slots. First live run on `www.leeserie.com` produced three `serp.absent` with details `not in the top 9 / 6 / 7 organic results` — so ranks 11–30, the entire `serp.page_two` band, are outside anything the payload contains. Probed directly on `rice cake cleanser`: `organic=7` by default, and `organic=7` again with `&num=30` appended, because Google no longer honours `num`. Consequence: a client ranking #11 and one ranking #500 produce a byte-identical finding, and the one band that maps to a T1 copy fix (close, needs a better title) is unreachable. | Not fixed — the choice costs money and is the operator's. **(a)** Accept page-one-only: delete `SERP_REACHABLE_MAX`/`serp.page_two`, rename the remaining code to say what is actually measured ("not on page one"), and drop the band from the CHANGELOG. One request per query, honest about its resolution. **(b)** Paginate with `&start=10` and `&start=20` to cover 1–30, restoring the band at **3× the per-query cost**. Note the detail string is already truthful (`not in the top 7 organic results` names its own denominator) — the defect is the unreachable band and the docs that advertise it, not a false claim in the artifact. **Reconfirmed at scale 2026-08-07:** the 39-query lee run produced 31 `serp.absent` and the denominators ranged `top 6` to `top 9` across all 31. Not one payload carried ten organic results, let alone thirty. |
| B-021 | 2026-08-07 | `pipeline/audit/providers.py` `serp_findings` | **A transient Bright Data failure silently churns the ratchet.** `serp_findings` calls `_request` once per query and drops any query that errors into `failed`, reporting `partial: N/M`. The failures are **transient, not deterministic**, so the set of measured queries differs run to run — and the query is `Finding.context`, which is fingerprinted. A query that fails this cycle and succeeds next appears as **NEW**; one that succeeds then fails **vanishes and reads as RESOLVED**, which is a fix that never happened being reported as a fix. First 39-query run on `www.leeserie.com`: `partial: 31/39 (8 failed)`. Re-probed two of the eight by hand immediately after: `best pregnancy belly cream` → `JSONDecodeError: Expecting value: line 1 column 1` (Bright Data answered with something that was not JSON), `is it safe to use skincare products while pregnant` → **succeeded, `organic=9`**. Same credentials, same zone, minutes apart. At 8/39 the failure rate is ~21%, so on a 39-query list roughly eight findings churn per cycle in each direction. Invisible in the artifact: `providers.serp` names only the first three failed queries, and `plan.py` has no way to tell "not measured" from "no longer a problem". | Not fixed. The minimum fix is a bounded retry in the per-query loop (one or two attempts with a short sleep) — the re-probe suggests most failures clear immediately. That reduces churn but cannot eliminate it, so the **structural** fix is for `plan.py` to treat a query absent from `providers.serp`'s measured set as *unmeasured* rather than resolved. That needs the provider to record WHICH queries were measured, not just how many — the status string currently truncates the failed list to three names. Worth doing before anyone reads a SERP RESOLVED as a win. |

## Fixed

| ID | Found | Fixed | Where | Symptom | Proof |
|---|---|---|---|---|---|
| B-131 | 2026-09-14 | 2026-09-14 | `pipeline/scanner/onpage_audit.py` (`CHECKS`, `parse_onpage_checks`, `site_audit_full`) | **Crawl Issues on DataForSEO was blank after a clean crawl, and two checks were mislabelled.** (1) A problem check with zero affected pages produced no row, so 25 pages with 0 broken / 0 4xx / 0 5xx / 0 orphan read as nothing. (2) `duplicate_title_tag` ("page with more than one title tag", docs.dataforseo.com/v3/on_page-pages) was labelled "Duplicate titles" (pages sharing a title); `duplicate_meta_tags` likewise. (3) The status counted passing rows as "flagged" ("12 check(s) flagged" was 9 problems + 3 passes). | A reported problem check with 0 pages is an `ok` row "0 of N page(s)"; a check DataForSEO did not report gets no row. Labels corrected. Status "N flagged, M passed". Live, 10 pages, $0.0015: `is_4xx_code / is_5xx_code / is_broken / is_orphan_page` → ok "0 of 10 page(s)"; `crawled 10 page(s), 8 check(s) flagged, 35 passed`. `tests/test_scanner_onpage_audit.py` (+4). |
| B-130 | 2026-09-14 | 2026-09-14 | `web/components/dashboard/ReportTable.tsx`, `web/app/ReaiDashboard.tsx` | **Crawl Issues read "No data here yet / Run a scan" right after a completed scan.** The operator's 10:05 run: Site Health crawled 25 pages, flagged 12 checks, $0.0037, saved. All 12 were on-page/technical flags, so the DataForSEO Crawl Issues view correctly had 0 rows, and the empty state claimed nothing had been scanned. The free crawl's duplicate titles and descriptions were on the same report, hidden behind the other source. | New `sourceRan()` in `web/lib/toolSources.ts`; the empty state now reads "No crawl issues found by DataForSEO (paid). The last scan checked this. Our tools (free) found N issues." with a button to switch source. `toolSources.test.mjs::a scan that ran and found nothing is not 'no data yet'`, built from the operator's saved report shape. |
| B-128 | 2026-09-14 | 2026-09-14 | `web/app/ReaiDashboard.tsx` (budget badge, drawer, budget card) | **"DAILY BUDGET: $0.00 / $5.00" after real spend.** Every budget display used `toFixed(2)`; DataForSEO calls cost fractions of a cent, so the $0.0037 the operator's Site Health scan saved to `scans.cost` read as $0.00. The ledger was correct; only the display rounded it away. Cross-checked: saved scans today sum to $0.0037, DataForSEO balance fell $0.0285 (the other $0.0248 was two direct scanner calls made to prove the fix, which bypass `/api/scan` and are never recorded, as B-120 describes). | New `formatUsd` in `web/lib/budget.ts` (4 decimals under a cent, 3 under a dollar). `spendSafety.test.mjs::the budget badge shows sub-cent spend instead of rounding it to $0.00`. |
| B-129 | 2026-09-14 | 2026-09-14 | `web/components/dashboard/AuditHeroBar.tsx` | **One click on "Try quick sample: example.com / wikipedia.org / github.com" started a scan immediately**, of a site that is not the client's, filed under the open project (B-126). The component also defaulted `currentDomain` to `"example.com"`, so the empty state read "Run a scan to measure example.com". | Sample buttons removed; default domain empty. `spendSafety.test.mjs::no one-click sample buttons start a scan of someone else's site`. |
| B-127 | 2026-09-14 | 2026-09-14 | `pipeline/scanner/server.py` (`/scan` handler) | **Every scan of a project whose repo is a GitHub `owner/name` created that path as a folder in the scanner's working directory**, ran `git init`, committed "scan: baseline" and wrote a client config and audit into it. `run_cycle(Path(repo))` read the slug as a relative path. Found when `DonghuaOnly/student-attention-tracking/` appeared in both checkouts during a live scan and was nearly committed as an embedded repo. Pre-existing. | New `local_checkout(repo)`: the plan cycle runs only for an existing absolute/explicit local directory, otherwise the log says why it was skipped. `tests/test_scan_progress.py::test_a_github_slug_is_never_treated_as_a_local_folder`, `::test_the_scan_handler_only_cycles_a_local_checkout`. The stray folders already created are untracked and excluded locally; not deleted. |
| B-113 | 2026-09-14 | 2026-09-14 | `web/app/ReaiDashboard.tsx` (Page Optimizer) | **Invented KPIs on a reachable screen**, reported by the operator from the live page: `{recs.length + 8} Ideas`, `+42% Lift`, `84 / 100` captioned as derived from the scan, `1-Click Ready`, an unconditional "All 3 Core Web Vitals Passed" above three "Not measured" tiles, a zero-filled content benchmark ("-0 words (Deficit)"), invented `statusCodes 92/5/3`, and passing rows counted as ideas. | Values derived from the report or removed; benchmark panel gated on a measured target. `fabricationSweep.test.mjs::the Page Optimizer shows no invented KPI (B-113)` and `::the content benchmark panel is gated on a measured target (B-113)`, both shown failing on the previous file and passing on the fix. |
| B-118 | 2026-09-14 | 2026-09-14 | `web/components/dashboard/SectionScanButton.tsx` | **React console error on every tool page:** "Removing a style property during rerender (borderStyle) when a conflicting property is set (border)". The bar's base style used the `border` shorthand; three states override `borderStyle`. Present on main; reported from the live page. | Longhands. `toolSources.test.mjs::the scan bar never mixes the border shorthand with a borderStyle override`. |
| B-119 | 2026-09-14 | 2026-09-14 | `pipeline/scanner/{dataforseo,server,audit,rows}.py`, `web/app/{ScannerApp,ReaiDashboard}.tsx`, `web/lib/{reportViews,reportMerge,toolSources,fixAdvisor}.ts`, `SectionScanButton.tsx` | **Defects the 2026-09-14 review of this branch confirmed**, most exposed by removing the spend latches: generic scan buttons ran all 8 paid tools with no cost shown and no in-flight guard; "did not run" rows read as a #1 keyword, listed in the Executive Report and sent to Claude; DataForSEO in-body refusals (40104) parsed as data and made into warn findings; runtime failures left pages silent; kill switch failed open on `1 # comment`/`true`; `.env` market ignored; an unreachable page wiped paid groups; a later scan erased Site Health rows; an unknown catalog hid free rows; "Scan all" billed unnamed paid tools; AI Mentions paid for rows it never shows. | `tests/test_spend_gate.py` (+20: in-body refusal, top-level refusal, refused card invents nothing, truncated body, runtime failure row, kill-switch values, market read per call, unreachable page keeps paid groups), `web/tests/spendSafety.test.mjs`, `reportMerge.test.mjs`, new cases in `reportViews`, `fixAdvisor`, `toolSources` tests. `pytest -q` → 1297 passed, 2 skipped; `npm test` → 450 pass; `tsc` clean. |
| B-106 | 2026-09-14 | 2026-09-14 | `pipeline/scanner/server.py` (`_wanted`), `pipeline/scanner/dataforseo.py` (`call`) | **Every paid tool was dark in a live scan, silently, for a week.** Two latches from d64662d: `_wanted` admitted a `dataforseo` tool only when `PYTEST_CURRENT_TEST` was set, and `call` refused any credentials except the test fixture pair `x`/`y`. A scan asking for Backlinks returned no Backlinks group and no reason, so 13 screens (rankings, keywords, competitors, backlinks) read as "nothing found". The source lane had the same shape: selected without a GitHub token, it was filtered out with no row (D12). Proved live against the running scanner before the fix: `tools=["seo","backlinks","rankings"]` produced `RESULT groups: ['seo']`. | One gate, `dataforseo.availability()`, read by both `call` and the catalog: credentials set and `DATAFORSEO_PAUSE_SPEND` not `1`. A selected tool that cannot run emits one ungraded `unavailable.<tool>` row naming the reason; `/tools` reports `available` + `unavailable_reason`. `tests/test_spend_gate.py` (17 tests, including a real-credential request reaching a stubbed `urlopen` and a paid tool running with `PYTEST_CURRENT_TEST` removed). |
| B-107 | 2026-09-14 | 2026-09-14 | `pipeline/scanner/dataforseo.py` (`rankings`, `keywords_card`) | **Composite cards threw away their sub-calls' status.** Each bound it to `_s` and ignored it, so a card whose every call was refused reported "0 row(s)", indistinguishable from a clean site. `keywords_card` with no project keywords silently skipped five sub-tools. | Sub-call refusals become `unavailable.rankings` / `unavailable.keywords` rows and are named in the card status; missing keywords and missing competitor are named. `test_spend_gate.py::test_rankings_surfaces_a_skipped_sub_call`, `::test_keywords_card_without_project_keywords_says_so`. |
| B-108 | 2026-09-14 | 2026-09-14 | `web/app/ScannerApp.tsx` (`run`), `web/app/api/scan/route.ts` | **Scan refusals never reached the screen.** The reader never checked `res.ok` and parsed only newline-terminated lines; every refusal the route sends (401, 400, 429 budget, 429 rate limit) is one JSON object with no newline, and "backend unreachable / SCAN_TOKEN unset" was sent with **status 200**. `X-Scan-Blocked-Tools` was set and read by nobody. B-104's "nothing happens", reachable five more ways. | New pure `web/lib/scanStream.ts` `readScanStream`; `run()` uses it (call site asserted); unreachable backend now 503; budget-skipped tools are written to the live log. `web/tests/scanStream.test.mjs` (13 tests over real `Response` objects). |
| B-110 | 2026-09-14 | 2026-09-14 | `pipeline/scanner/server.py` (run loop) | **Site Health overwrote the free crawl's findings.** Both file under `report["site"]`; the loop assigned `groups[t.key] = rows`, so selecting Site Health with a multi-page crawl discarded every `site.*` finding. | `groups.setdefault(t.key, []).extend(rows)`. `test_spend_gate.py::test_site_health_does_not_overwrite_the_free_crawl`, shown failing with the assignment restored (`AssertionError: ['dfs.op.is_orphan_page']`) and passing with the fix. |
| B-111 | 2026-09-14 | 2026-09-14 | `web/app/ScannerApp.tsx` (`sourceOf`) | **Our free on-page results were labelled "DataForSEO — live data".** `health.*` is emitted by the free On-page SEO tool (`pipeline/scanner/audit.py`), but `sourceOf` grouped it with `dfs.*`, so while DataForSEO was switched off our own findings still looked like paid ones. | `health.*` → "Our on-page checks (free)"; `unavailable.*` → "Not run". `scanStream.test.mjs::our free on-page rows are never labelled as DataForSEO`. |
| B-112 | 2026-09-14 | 2026-09-14 | `web/lib/sectionScans.ts` (`VIEW_TOOLS`) | **Domain Overview's scan button ran the wrong tool** (`keywords`, $0.18; `dfs.domain_overview` comes from `rankings`), and **Core Web Vitals ran two of the four Lighthouse categories** its own blurb names. | `domain-overview` → `rankings`; `core-web-vitals` adds `lh_a11y`, `lh_bp`. New `reportViews.test.mjs::every scanner tool's 'did not run' row reaches a screen`. |
| B-105 | 2026-09-14 | 2026-09-14 | `web/app/ReaiDashboard.tsx` (`trafficAnalytics.monthlyTrend`, `ExecutiveTrafficChart`) | **"Mar '26: 0K visits" shown in September.** The six-month traffic trend was one modelled estimate multiplied by 0.65..1.0 under hardcoded Oct-Mar labels, and the chart appended a hardcoded `'26`. Also invented: `devices {36/64}`, `uniqueVisitors = visits*0.72`, prop defaults `"6.2K"` and `128` (twice). | Series emptied so the chart renders "Not measured"; constants and defaults removed. `tests/fabricationSweep.test.mjs` "the traffic chart draws no history that was never measured (B-105)"; `npm test` 408 pass. |
| B-104 | 2026-09-13 | 2026-09-13 | `web/app/api/{scan,plan,remediate,remediate/dryrun,remediate/apply}/route.ts` | **Every scanner POST from the web UI was answered 403, and the UI showed nothing.** B-079 added an `X-Scan-Token` guard to the scanner's `do_POST`; no Next proxy route was ever given the token, so the whole measure/plan/remediate spine was dead from the browser. It failed invisibly for two further reasons: the 403 body is one NDJSON-parseable line so the client read it as a stream event, and the error was written to `ScannerApp`'s `data`, which is not a dashboard prop — `busy` went true then false and nothing was drawn. A third defect rode along: `AbortSignal.timeout(10000)` on the streaming fetch bounded the BODY, tearing a real scan down 10s in. Reported as *'I click audit and it like loading for 500ms then nothing happen.'* | **Fixed 2026-09-13.** One chokepoint `web/lib/scannerFetch.ts` (`scannerPost`/`scannerGet`) attaches the token and clears the header timeout once headers arrive; `scanState.error` reaches `SectionScanButton` as a `role="alert"`. `web/tests/scannerFetch.test.mjs` — 6 tests, incl. the header proved over a real socket and a sweep asserting no route calls `fetch(`${PYTHON_API}...)` directly (that sweep caught `app/api/tools/route.ts` as a second site). Requires matching `SCAN_TOKEN` in repo-root `.env` and `web/.env.local`. |
| B-103 | 2026-09-13 | 2026-09-13 | `web/components/dashboard/RepoPicker.tsx` + 15 more sites across 5 files | **"Unauthorized: Missing or invalid Supabase authentication token" - and no repositories listed.** Reported by the operator immediately after connecting GitHub. `RepoPicker` called `/api/github/repos` with a plain `fetch`. The Supabase session lives in `localStorage`, not a cookie, so a bare fetch carries no identity and every route guarded by `authenticateRequest` answers 401. Reproduced directly: `curl -s localhost:3001/api/github/repos` -> `{"error":"Unauthorized: Missing or invalid Supabase authentication token."}`. **A 401 renders as an empty list**, so the screen reads "you have no repositories" when it means "we never asked properly". **The same bug had already shipped three times** - `ContentPanel` (which 401'd every content tool in production), `RepoPicker`, and thirteen further sites found only by grepping for the pattern: sixteen across `ReaiDashboard.tsx`, `ScannerApp.tsx`, `integrations/google/page.tsx`, `GateActivity.tsx` and `RepoPicker.tsx`. It is invisible to review: `fetch(` and `authedFetch(` differ by six characters and behave IDENTICALLY in development, where `ALLOW_DEV_AUTH` masks the difference; it fails only in production, silently, as an empty screen. | Applying the thermonuclear standard's Rule 0 - delete the class rather than rearrange it, since patching sixteen call sites leaves the seventeenth free to happen. All sixteen converted to `authedFetch`, and `web/tests/apiAuth.test.mjs` fails the build on a bare `fetch("/api/…")` anywhere under `app/` or `components/`, excluding `app/api/**` (a server route calling another service is not a browser fetch and has no session). The wrong thing is now impossible rather than currently absent. | `web/tests/apiAuth.test.mjs` (3): no client code reaches our API with a bare fetch; `authedFetch` attaches the session and survives having none; the four routes we depend on all carry `authenticateRequest`. `npm test` -> **380 passed**; `npx tsc --noEmit` -> clean; `python -m pytest -q` -> 1261 passed, 2 skipped. |
| B-098 | 2026-09-13 | 2026-09-13 | `web/app/api/plan/route.ts:281` | **A traffic forecast computed from the number of to-do items, shipped in the client-facing plan response.** `projectedLift: `+${Math.min(28, Math.max(6, sprintItems.length * 2.2)).toFixed(1)}% Organic Visibility``. Zero items promised +6.0%, because of the `Math.max(6, …)` floor; fifty promised +28.0%. It rode alongside the executive summary and the developer brief, which makes it the most publishable fabrication in the codebase - a figure a client puts in a deck and an agency gets held to. | Removed, not replaced. Forecasting organic lift needs a baseline, a control and a measured outcome; this product has none of the three, so there is no honest version of this field. Verified consumed by nothing (`grep -rn projectedLift` -> the definition only). | `web/tests/fabricationSweep.test.mjs::no screen forecasts a traffic lift` walks every `.ts`/`.tsx` under `app/`, `lib/`, `components/` and fails on `projectedLift:` or `% Organic Visibility`. |
| B-099 | 2026-09-13 | 2026-09-13 | `web/app/api/traffic/snapshot/route.ts:100`, `web/app/ReaiDashboard.tsx` (device card) | **Fabricated data written into the database.** `devices = { mobile: 68, desktop: 32 }` was a default destructure that was then INSERTed into `traffic_snapshots` (`:117`, `:141`) and read back later as history. Every other fabrication in this codebase is a render-time lie that a delete removes; this one was durable, and would have been indistinguishable from a real measurement on any later read. The UI half matched: `gscDeviceSplit` initialised to the same 68/32, and its setter only fires inside `if (sum > 0)`, so a failed or empty device query left the invented split on screen under a green **"Google GSC Verified"** badge, above a hardcoded **"Mobile-first indexing compliant ✓ Verified"** - a claim nothing in this codebase measures. | Route default is `{}`. State is `null`. When null the card draws **no donut and no bars at all** rather than a zeroed chart, because a chart is a claim; the badge reads "Not measured" and the footer says mobile-first indexing is not measured by this product. | `fabricationSweep.test.mjs::no invented measurement is written to the database`. |
| B-100 | 2026-09-13 | 2026-09-13 | `web/app/ReaiDashboard.tsx` (SERP presets, AI optimizer, Magic Tool brief, schema modal, recommendations fallback) | **One pilot client's copy rendered for every account - a confidentiality problem as much as a fabrication one.** Eleven occurrences of "Phnom Penh"; `"Ministry of Health Cambodia"` listed as a client's semantic entity; `"50+ board-certified international consultant physicians"`, an invented credential claim of exactly the class `claim_provenance_check` refuses on a client's site; `MedicalOrganization` schema, NICU and obstetrics descriptions. The SERP Optimizer simulated "Best Hospital & Medical Center in Phnom Penh" for every client **on the screen whose entire purpose is showing the operator what Google displays for their own page** - and `selectedOnPageUrl` defaults to `"/"`, so it was the first thing every account saw. The "AI CTR Optimizer" button contained no AI: a lookup table of four hardcoded hospital titles keyed by URL, with a medical catch-all for every other page. | The scan now carries `report.page` (see B-102), so the preview shows the real title and description and says so when there is none. The optimizer calls the same Claude route every other feature uses, grounded in that page, and is disabled when there is no page to rewrite. Invented entities, headings and `schemaType` removed; only the keyword and the business name survive, both known. | `fabricationSweep.test.mjs::no pilot client's vertical is hardcoded anywhere` greps every source file, comments stripped, for six markers. |
| B-101 | 2026-09-13 | 2026-09-13 | `web/app/api/plan/route.ts` (3 sites) | **The developer brief guessed a file path in the client's repository.** `targetFile` was derived from the finding code: `components/SEOHead.tsx` for anything mentioning canonical/meta/title, `src/app/layout.tsx` for performance, and for anything mentioning schema, `components/MedicalBusinessSchema.tsx` - one client's vertical again. Nothing in this route reads the client's repository, so the brief told a developer to edit files that may not exist. A guessed path is worse than no path: it reads as though we looked. | `targetFile: null`, and the brief renders "Not known from the scan. The finding is on the live page; locate the source that renders it." `wf-site-remediate` knows real paths because it works inside the checkout and measures the git diff; this route does not. Missing `why`/`fix` also stopped being filled with generic filler presented as a recommendation. | `fabricationSweep.test.mjs::no file path is guessed for the client's repository`. |
| B-102 | 2026-09-13 | 2026-09-13 | `pipeline/scanner/server.py`, `web/components/dashboard/ContentPanel.tsx`, `web/app/ReaiDashboard.tsx` | **`ContentContext.page` was declared, read by five of the seven content tools, and populated by nobody.** `pageBlock()` bails on its first line when `page` is absent, so "Depth Expansion" - whose blurb reads *"Rewrites the scanned page"* and whose own comment says "the optimizer used to ask the operator to paste content the scan was holding all along" - received nothing and rewrote nothing unless the operator pasted it back in by hand. B-007's shape for the fifth time, and `tests/contentTools.test.mjs` passed throughout because it constructs `page` itself. The root cause was upstream: the scan report carried only VERDICTS about the page, never the text, so the SERP preview had nothing real to show either and invented it (B-100). | New `page_facts()` in the scanner returns the page as fetched - title, description, canonical, heading outline, word count, text (capped at 12k) - read from HTML already in hand, so it costs nothing and needs no credential. `report.page` is threaded to `ContentPanel` and to the SERP preview. Two further defects fixed in the same component: it used a plain `fetch` while the session lives in localStorage, so **every content tool returned 401 in production** (verified live: `POST /api/content/generate` with no auth header -> 401); and `if (!tool) return null` sat above four `useMemo`/`useEffect` calls, a rules-of-hooks violation that React punishes only intermittently. | `fabricationSweep.test.mjs::the content tools are given the page the scan fetched`, `::the content panel sends the session`, `::hooks run before any early return`. `python -m pytest -q` -> **1261 passed, 2 skipped**; `npm test` -> **361 passed**; `npx tsc --noEmit` -> clean; `npm run build` -> succeeds. |
| B-097 | 2026-09-13 | 2026-09-13 | `web/app/ReaiDashboard.tsx` -> `web/components/dashboard/GbpMatrix.tsx` | **The Google Business Profile matrix asserted four verdicts it had never measured, and drew a chart from invented data.** `const isClaimed = claimedFinding ? claimedFinding.severity !== "warn" : true` - **absence meant CLAIMED**, so an account that had never run the Local tool was told its profile was claimed and active. Beside it: `"Verified"` printed unconditionally next to a status that could read "Needs Setup"; `"Trust Signal"` next to a rating that could read "Not measured"; `"Rank #1"` next to a primary category, when nothing measures a category's rank and a category is not a ranking at all; `MiniRadialGauge score={... ? 100 : 50}`, a picture of a number nobody computed; a hardcoded `🏥` for every client whatever their sector; and `MiniSparkline data={[4.1, 4.3, 4.5, 4.6, 4.7, 4.8]}` - **an invented six-point rating trend rendered as a real chart**, when one scan is a point rather than a line and nothing in this product stores a rating history. The `Live Signals` badge was green whether or not anything had been measured. Same family as B-094 and B-096; found by the operator reading the screen. | Extracted to `GbpMatrix.tsx` and rebuilt on three states rather than two: measured-and-fine, measured-and-not, and not measured. Nothing unmeasured is green, and nothing unmeasured is DRAWN - the gauge and the sparkline are omitted entirely rather than fed a stand-in, because a chart is a claim like any other. The extraction was forced rather than chosen: mounting the new Fix-with-Claude panel pushed `ReaiDashboard.tsx` to 13,031 lines and the "shrank below 13,000" ratchet went red; the file is now 12,829. | `npm test` -> **341 passed**; `npx tsc --noEmit` -> clean. Greps in `web/tests/localAndGate.test.mjs` fail the build if `4.1, 4.3, 4.5`, `>Rank #1<`, `>Trust Signal<` or an unconditional `>Verified<` return. |
| B-096 | 2026-09-13 | 2026-09-13 | `web/app/ReaiDashboard.tsx` (Local Presence: directory matrix, findings checklist, sentiment split, map embed) | **The Local screen was almost entirely invented, and the operator spotted it by eye** - "i feel like it fake, is it real". Six directory tiles printed `Google Maps Synced · Apple Maps Synced · Bing Places Synced · Waze Local Synced · Yelp Biz Claimed · YellowPages Format Diff` under a `5/6 Verified Active` badge, every value a literal. **Nothing in this codebase has ever queried Apple Maps, Bing Places, Waze or YellowPages**, and four of the six publish no read API at all, so no tool at any price could produce those verdicts. Beneath it the findings checklist used the same find-or-fabricate shape as B-094: `(gbpRows.length > 0 \|\| mentionsRows.length > 0) ? real : [six invented rows]`, and those rows carried **"4.8 / 5.0 rating across 184 Google reviews. 94% positive sentiment ratio."**, a primary category of "Medical Center / Hospital", and "Sync telephone format with E.164 standardization (+855...)" - one pilot client's rating, category and country code, shown to every account. A rating and a review count that trace to nothing is precisely the claim `claim_provenance_check` blocks on a CLIENT's site; our own dashboard printed it. Three more in the same view: `85% Positive / 12% Neutral / 3% Negative` as literals (the bar above them had already been emptied, with a comment saying so - so the chart was honest and the numbers beside it were not), `sentimentFinding?.detail \|\| "90% Positive"`, and `Google Map Embed & Coordinates — Ready ✓` rendered unconditionally though nothing reads a geo value or looks for a Maps iframe. | New `web/lib/localSignals.ts` derives the matrix and distinguishes five states: checked-and-ok, checked-and-problem, **not checked**, **not built** (Yelp - the Fusion API exists and would work, it just is not wired), and **no public API** (Bing Places, Apple Business Connect, Waze, YellowPages). Nothing unmeasured is ever green. The count is derived and names its denominator honestly: "1/1 verified · 4 cannot be checked by any tool". The checklist renders an empty state naming what the Local tool actually reads (rating, review count, primary category, address, phone, claimed status, all from the live Google Business Profile). Sentiment and the map embed both read "Not measured" with a sentence explaining that no sentiment split is computed. | `web/tests/localAndGate.test.mjs`: a directory nobody can query is never "synced" and never green; Google is "Not checked" before a scan and takes a real verdict after, with one failing signal making the whole profile "needs work"; Yelp says what would make it work; the count is "None checked yet" with no data; every note explains why; and a grep fails the build if any of nine invented strings return. `npm test` -> **315 passed**; `npx tsc --noEmit` -> clean. |
| B-095 | 2026-09-13 | 2026-09-13 | `web/app/ReaiDashboard.tsx` (Answer Content tiles, AI Crawler Access table, recommended robots.txt) | **Two AEO sub-views invented their numbers, and the crawler table actively misinformed the client.** (1) *Answer Content* showed `84% Ready / 12 Question Headings Detected`, `42 Words / Optimal for Direct LLM Quoting` and `Enabled / Enables Google Accordions` - six literals, identical on every account. Two could not have been real in principle: `aeo.no_answer_structure` is a BOOLEAN and the scanner measures neither a heading count nor an answer length, so no number existed to display. (2) *AI Crawler Access* listed six bots with verdicts and never read a robots.txt - and it described `GPTBot` as "Required for ChatGPT citations" and `ClaudeBot` as "Required for Claude Search". **Both false.** Both are TRAINING crawlers; the citation bots are `OAI-SearchBot` and `Claude-SearchBot`, under separate directives. The engine has had the correct three-class split since B-080 (`pipeline/gates/robots_aicrawler_check.py`, with `tests/test_robots_aicrawler_check.py::test_the_three_classes_do_not_overlap`); the UI contradicted its own engine, in the direction that makes a client afraid to opt out of model training. (3) The *recommended robots.txt* allowed `GPTBot`/`ClaudeBot`, named none of the four bots that decide citability, `Disallow`ed training crawlers on the client's behalf, and emitted `Sitemap: https:///sitemap.xml` with no client selected - a broken URL in a file that goes live. | New `web/lib/aeoCrawlers.ts` mirrors the gate's citation/training/user classification, derives each status from the scan rows (`null` when unmeasured; `aeo.robots_missing` is NOT an allow; user-triggered bots are never graded), names the true consequence of blocking each bot, and never colours a blocked training crawler red. `buildRobotsSnippet` is generated from that same list so it cannot drift: citation bots allowed, training bots commented out as the client's decision, and no Sitemap line when there is no domain. The three Answer Content tiles now report the two verdicts that exist and say plainly that heading count and answer length are not measured. | `web/tests/aeo.test.mjs`, 25 tests total: `GPTBot`/`ClaudeBot` are training and `OAI-SearchBot`/`Claude-SearchBot` are citation; every training consequence states citations are unaffected; no crawler claims a status over no data; a blocked crawler is read from the row detail while its siblings read allowed and an unmeasured class reads null; a missing robots.txt is not an allow; blocking a training crawler is never coloured as a failure; the snippet allows all five citation bots and comments out all four training bots; and `buildRobotsSnippet(null)` never contains `https:///`. Plus greps that fail if any of the seven invented strings return. `npm test` -> **295 passed**; `npx tsc --noEmit` -> clean; `npm run build` -> succeeds. |
| B-094 | 2026-09-13 | 2026-09-13 | `web/app/ReaiDashboard.tsx` (AEO tiles, signals matrix, per-engine grid, Overview panel) | **The AI Search Visibility screen delivered a completed audit of a site nothing had scanned.** Reported by the operator: sign in, run nothing, and the screen reads `Allowed ✓` / `Valid Schema ✓` / `Detected ✓` with a five-row PASS/WARN matrix under it. Every verdict was a binary over a `.find()` result, so `undefined` - the check never ran - fell through to the pass branch: `{crawlerBlocked ? "Blocked" : "Allowed ✓"}`, `{schemaBiz && schemaBiz.severity !== "ok" ? "Needs Fix" : "Valid Schema ✓"}`, `{answerStruct && ... : "Detected ✓"}`. Four fabrications from that one cause: (1) the three tiles; (2) the signals matrix, which substituted **five invented rows** when there were no real ones - three marked PASS, one reading *"Verified in robots.txt"* for a robots.txt nobody had fetched; (3) the per-engine grid, three statuses off the same falsy check and the fourth hardcoded as the literal `"Snapshot Ready"` for Google AI Overviews - a claim about AI Overview eligibility nothing here measures - under a heading promising "Real-time extraction and citation probabilities" that it does not compute; (4) a **second copy on the Overview**, hardcoded outright: `4/4 Ready`, four engine statuses, `Robots.txt AI Crawlers 4/4 Allowed (100%)`, `LocalBusiness JSON-LD Missing (Action Req.)`. Same family as B-053 and B-051: a component rendering a state instead of reading one. What makes it worse than those is the subject - this is the engine's own never-green rule (B-018, B-027, where `forbidden_sweep` and `audit_ssr` exit 4 for "cannot judge") broken at the last mile, where it actually reaches a person. | New `web/lib/aeo.ts`. `deriveAeoTiles` returns three states - `null` (no row carries this verdict), `"ok"`, `"problem"` - and the UI renders `null` grey and unticked with the sentence that would produce a verdict. A tile is green only when EVERY matching row is `ok`, and only the literal `"ok"` passes, so an unrecognised or misspelled severity cannot fall through. `aeoMatrixRows` returns `null` rather than a substitute table, and the caller renders an empty state. The engine card is renamed "AI Crawler Access By Engine" and says "Access is a precondition for citation, not a measure of it". The Overview copy became `AeoAccessPanel`, reading the same derivation. | `web/tests/aeo.test.mjs`, 12 tests: no tile claims a verdict over `null`/`[]`/junk; no tick and no pass-green when unmeasured; every unmeasured tile names the next step; a failing row among passes is still a problem; `"error"`, `""`, `undefined`, `"OK"` and `"pass"` are all refused as passes; **a scan that measured crawlers but not answer structure leaves the answer tile unmeasured** (the original bug's exact shape); no rows means no table; and greps that fail if any of the five invented rows, `"Snapshot Ready"`, the hardcoded statuses, or the old `crawlerBlocked ?` binary return. `npm test` -> **282 passed**; `npx tsc --noEmit` -> clean. |
| B-093 | 2026-09-13 | 2026-09-13 | `pipeline/scanner/extra_checks.py` (`video_rows`) | **The Video report view was empty on exactly the pages that have video, and the video remediation never fired.** Reported by the operator: the Video section printed its empty hint ("Run a scan...") with a valid `YOUTUBE_API_KEY` loaded and a scan already complete. `extra_checks.py` binds its module-level `_row` to the `tech` prefix - almost everything in the file is a technical check - and `video_rows` inherited that prefix purely by living there, so the VideoObject schema row was stamped **`tech.video_snippets`**. **Why no test caught it:** the case everyone exercises never reaches the function. `youtube.video_rows_full` answers "no video on this page" with its own `video.`-stamped row and returns before calling in, so the pass path was correct; only a page that ACTUALLY has a video took the broken branch. Two consequences, both silent: (1) `web/lib/reportViews.ts` filters the Video view on `codes: ["video."]`, so the schema row rendered under **Technical** and the Video view showed only the metadata row - or nothing at all when there was no key or the fetch errored, printing the empty hint over a finished scan; (2) `pipeline/scanner/recommendations.py` keys the finding as `video.video_snippets`, with a comment naming that exact prefix, so a `tech.`-stamped row matched no recommendation and the fix was never offered. The one case the tool exists for was the broken one. | `video_rows` now uses its own `_video_row = make_row("video")`. It is called by nothing but the Video tool (`grep -rn 'video_rows\b' pipeline tests` -> only `youtube.py` and its own tests), so the prefix move touches no other view. | Live, after the fix: `video.video_snippets [warn] 1 embedded video(s) but no VideoObject schema` + `video.video_metadata [ok] 1 video(s) carry full metadata (title, description, duration, thumbnail)` - the second row from a real YouTube Data API v3 call. `tests/test_scanner_extra.py` gains 3 tests: every code from all four HTML shapes (no video, embed without schema, embed with schema, native `<video>`) starts with `video.`; the warn row's code exists in `RECOMMENDATIONS`; and `video_rows_full` as a whole emits exactly `{video.video_snippets, video.video_metadata}` - it composes two modules that were stamped differently, so testing either half alone would have missed this. **Restoring the old prefix turns all 3 red.** `python -m pytest -q` -> **1251 passed**; `ruff check pipeline tests` -> clean. |
| B-092 | 2026-09-13 | 2026-09-13 | operational — `web/.next` shared by `next dev` and `next build` | **The app froze on "Checking authentication..." forever, and the 600 ms safety timeout that exists to prevent exactly that never fired.** `npm run build` writes a PRODUCTION tree into the same `web/.next` a running `next dev` is serving from. Running `rm -rf .next && npm run build` while a dev server is live therefore deletes its compiled output and replaces it with artifacts it cannot use. The HTML still renders - the server-side pass emits the loading state - but the client bundle is gone: `curl` of the chunks listed in that HTML returned `404 /_next/static/chunks/polyfills.js`, so React never hydrated, `AuthGate`'s `useEffect` never ran, and the `setTimeout(600)` inside it never existed on the page. The screen was a static server-rendered string with no JavaScript behind it. The dev server's own log named it: `MODULE_NOT_FOUND ... at .next/server/app/api/tools/route.js`. **Two `next dev` instances were also running against that one directory** (ports 3001 and 3002, both `cwd=/Users/both/seo_agent/web`), which compounds it - they overwrite each other's compilation. Same class as B-056: a stale or wrong process serving code that does not match the disk, with a green test suite and no error the UI can show. | Killed both dev servers and their parents, `rm -rf .next`, started ONE `next dev`. **Never run `npm run build` against a `.next` a dev server is using** - use a separate directory or stop the server first. A verification build belongs in its own step, after the dev server is down. | One server listening (3001; port 3000 is a different project and was left alone). Every chunk referenced by the served HTML now returns 200, `polyfills.js` included. Unauthenticated smoke tests: `GET /api/auth/google/status` -> `connected:false` with no account details, `POST /api/gsc/query` -> 401, `GET /api/clients` -> 401. |
| B-091 | 2026-09-13 | 2026-09-13 (schema + **live DB applied**) | `web/supabase-schema.sql` (`traffic_snapshots_owner` policy) | **A per-user table was readable, writable and deletable by anyone holding the public anon key.** `traffic_snapshots` had RLS ENABLED and a policy called `traffic_snapshots_owner` whose body was `for all using (true) with check (true)`. Both signals a reviewer scans for - RLS on, policy present, named "owner" - were green; `using (true)` grants every row to every role for select, insert, update and delete. The anon key is public on purpose (it ships in the browser bundle, RLS is what protects the data), so this was a public endpoint. Measured from outside with that key on 2026-09-13: `clients`, `scans`, `findings` and `scan_tools` all returned `content-range: */0` while the service role saw 2, 2, 94 and 22 - RLS working. `traffic_snapshots` returned **`0-2/170`**, i.e. all 170 rows, carrying `site_url`, `clicks`, `impressions`, CTR, average position, country and device splits and the full `top_queries` array - the real search terms a customer's visitors typed. Nine sibling tables in the same file use `auth.uid() = user_id`; this was the only exception, and the application never needed it (the route at `app/api/traffic/snapshot/route.ts` already writes `user_id` and filters on it). All 170 rows additionally have `user_id` NULL, so they belong to nobody and no corrected policy could ever match them. | Policy replaced with `auth.uid() = user_id` for both `using` and `with check`; `user_id` made `not null`. `web/migrations/2026-09-13-b091-traffic-snapshots-rls.sql` deletes the 170 orphans first (the table is a regenerable Search Console cache), then alters the column, then swaps the policy, and ends with two verification queries - one over `pg_policy` for any policy on a `user_id` table whose `using` clause does not mention `user_id`, and one anon-key `curl` that must come back `content-range: */0`. **A schema fix in the repo changes nothing until it is applied.** The operator ran `web/migrations/RUN-THIS.sql` in the Supabase SQL editor on 2026-09-13; verified from outside immediately after with the PUBLIC anon key: `traffic_snapshots` went from `content-range: 0-2/170` to `*/0`, the 170 orphan rows are gone (`user_id=is.null` -> `*/0`), and all ten per-user tables now return `*/0` to that key. Real data untouched: clients 2, scans 2, findings 94, scan_tools 22, projects still Vertly and Orienda hospital. | `web/tests/rls.test.mjs`, 7 tests over the schema: RLS enabled and a policy present for every `user_id` table; no policy body contains `using (true)`; every per-user policy compares `auth.uid()` to `user_id`; every write policy carries a `with check` (a `using`-only policy still lets a caller insert rows under another user's id); no `user_id` is nullable; every view sets `security_invoker` (a Postgres view runs as its OWNER by default and bypasses the RLS of everything it reads). **Proof the guard bites:** restoring the original policy text turns 3 of the 7 red, and reverting turns them green again. `npm test` -> **255 passed**. |
| B-090 | 2026-09-13 | 2026-09-13 | `web/app/api/auth/google/status/route.ts`, `web/app/api/gsc/query/route.ts`, `web/lib/gbp.ts` + the four `web/app/api/local-seo/*` routes, `web/app/auth.tsx` | **Cross-account disclosure: one operator's Google Search Console and Business Profile were served to the next person who signed in to the same browser.** Reported by the operator - signed in with a different account, saw the same data. The Google connection lived entirely in cookies (`gsc_access_token` 30 days, `gsc_refresh_token` 1 year, `gsc_user_email`, `gbp_secondary_*`) and **a cookie belongs to a browser, not to a signed-in user.** Three faults compounded: (1) nothing tied those cookies to a Supabase account; (2) `handleSignOut` cleared the Supabase session and nothing else; (3) **none of the six routes reading them authenticated the caller** - `grep` for `cookieStore.get("gsc_access_token")` and `jar.get("gsc_access_token")` found them in `status`, `gsc/query` and `lib/gbp.ts`, and `gbpContext()` took no arguments at all, so all four `local-seo` routes inherited the hole from one function. Reproduction needs no attacker: connect Google as A, sign out, sign in as B in the same browser; the dashboard's mount-time `GET /api/auth/google/status` returns A's email, A's verified properties and A's site list, and every downstream screen reads from them. Five `reai_*` localStorage keys (selected property, account email, traffic source) survived the same way. | Two halves, because either alone leaves a hole. **Ownership:** new `web/lib/googleSession.ts` owns a `google_owner` cookie carrying the Supabase user id, and `googleSession(request)` is the only path to a token - it authenticates, compares, and on a mismatch DELETES the cookies rather than hiding them (a surviving token is one waiting for its owner to sign back in on a machine they have left). An unauthenticated request is refused but destroys nothing, so a forgotten bearer cannot sign the real owner out. **Sign-out:** `purgeGoogleConnection()` clears the cookies server-side and the `reai_*` keys locally, on sign-out and on any change of user - another tab, a token refresh onto a different account, or signing in over a live session all replace the session without a sign-out. Ownership is claimed on first authenticated read, not during the OAuth callback: the callback is a top-level redirect and this app's Supabase session lives in localStorage, so no session cookie and no Authorization header exist at that moment. That claim is BOUNDED by a `google_claim_pending` marker the callback alone sets, single-use and 5 minutes - without it the fix would have made things worse for the reporter, whose browser already holds an unowned leaked connection that an unbounded trust-on-first-use would have stamped as legitimately theirs. An unowned connection with no pending claim is unattributable: cleared, reconnect once. New `web/lib/authedFetch.ts` attaches the access token; 16 client calls moved onto it. `/api/gsc/query` additionally gained authentication, a rate limit, a 32 KB body cap, `siteUrl` validation, clamped `days`/`rowLimit`, and stopped echoing Google's error body (a 403 names properties and permissions). | `web/tests/googleIsolation.test.mjs`, 14 tests: a foreign connection is deleted not hidden; an unauthenticated request reveals nothing and destroys nothing; tokens are returned only in the `owned` state; sign-out purges BEFORE `setSession(null)`; an account swap without a sign-out purges too; a failed network disconnect still clears the local half; no client reaches a Google route with a bare `fetch`. Plus a sweep over every `.ts`/`.tsx` under `app/`, `lib/` and `components/` that fails if a Google token cookie is named outside the session layer - which is exactly how this happened, six places each reading the jar for themselves. `npm test` -> **243 passed**; `npx tsc --noEmit` -> clean; `npm run build` -> succeeds. |
| B-089 | 2026-09-13 | 2026-09-13 | 16 write sites across `pipeline/audit/`, `pipeline/lib/`, `pipeline/scanner/`, `pipeline/seed/`, `pipeline/outreach/` | **Every cycle artifact was written non-atomically, and a truncated one reads as a valid absence.** `Path.write_text` opens with `"w"`, which truncates to zero bytes, and only then writes. A process ending inside that window - Ctrl-C on a long measure, an OOM kill, an Actions step timing out, a full disk - leaves an empty or partial file, and **the next stage reads it without complaint**. An empty `findings.json` is not an error to `plan`; it is a site with no findings, so the ratchet files every real finding as RESOLVED, the next worklist is empty, and the cycle reports that everything got fixed. A truncated one raises a `JSONDecodeError` from somewhere unrelated to the write. A half-written `docs/gate-baseline.json` runs the whole fleet's gates bare - the same outcome B-007 already cost a release. Zero `os.replace` calls existed in the repo. | `pipeline/lib/atomic.py`: `write_atomic` writes a sibling temp file, fsyncs it, `os.replace`s it into place (atomic on POSIX and Windows, unlike `os.rename`) and fsyncs the directory so the rename itself is durable. Sibling rather than `/tmp` because a cross-filesystem rename is not atomic. `write_json_atomic` serialises before touching the file, so an unencodable document fails with the previous artifact intact. Cleanup catches `BaseException`, since `KeyboardInterrupt` is not an `Exception` and Ctrl-C is the interruption this is for. | `tests/test_atomic.py` (10): `a failed write leaves the previous artifact intact`, `an interrupt mid-write leaves the previous artifact intact` (patches `os.replace` to raise `KeyboardInterrupt`), `replacing an existing file never shortens it first` (reads the destination from inside a patched `os.replace` and asserts the old content is still whole), `the temp file is a sibling not in /tmp`, plus a guard that fails the build if any cycle artifact is written with a plain `write_text` again. `python -m pytest -q` -> **1248 passed**. |
| B-086 | 2026-09-13 | 2026-09-13 | all eight `<loc>` parsers and all four `<title>` parsers, now `pipeline/lib/html.py` | **Nothing decoded XML entities.** A sitemap MUST escape `&` as `&amp;` (XML rule, and the sitemaps.org spec repeats it), so every paginated, filtered or tracked URL on a real site is published escaped: `<loc>https://x.test/a?page=2&amp;sort=new</loc>`. All eight parsers returned the literal `&amp;`. Downstream, that URL is then **fetched** (404 -> reported as a broken link), **compared** against the built route list (mismatch -> reported as a parity failure) and **diffed** against internal links (no match -> reported as an orphan). Three gates producing three confident findings about one page that is completely fine, on any site with a query string in its sitemap. The `<title>` side had the same bug with a different consequence: `health.title_length` measured `Roof &amp; Gutter` as 17 characters against its 30-60 window instead of 13, and the duplicate-title check compared raw strings, so two pages whose titles differ only in escaping read as distinct. | One `decode_entities` (`html.unescape`, which leaves an unknown entity alone rather than raising - a page is better measured with one odd `&nosuch;` than not measured) applied inside `sitemap_locs`, `page_title` and `inner_text`. Case-insensitivity, namespace prefixes (`<sm:loc>`) and CDATA are handled in the same place, since half the old patterns were case-sensitive on a case-insensitive format and none handled the other two. | `tests/test_lib_html.py`: `test_an_escaped_ampersand_comes_back_as_an_ampersand`, `test_a_title_with_an_escaped_ampersand_is_measured_at_its_real_length` (asserts len 13), `test_two_titles_differing_only_in_escaping_compare_equal`. `python -m pytest -q` -> **1238 passed**. |
| B-087 | 2026-09-13 | 2026-09-13 | `pipeline/scanner/validate.py` | **The sitemap validity check matched half a tag.** Its pattern was `<loc>\\s*([^<\\s]+)` with no closing `</loc>`, so `<url><loc>https://x.test/a` in a truncated or malformed sitemap counted as a valid entry and the check reported the sitemap as fine. The row it emits is the one telling an operator their sitemap is well-formed. | Uses `sitemap_locs`, which requires the closing element. | `tests/test_lib_html.py::test_a_truncated_element_is_not_a_url`. |
| B-088 | 2026-09-13 | 2026-09-13 | `pipeline/audit/bootstrap_config.py` (`detect_topology`) | **Topology detection silently returned "TODO" for any client whose sitemap uses site-relative URLs.** The pattern was `<loc>https?://[^/]+(/[^<]*)</loc>` - it required an absolute URL in order to capture the path. Site-relative `<loc>` values are legal and several static generators emit them; on those sites `urls` was empty, `paths` was empty, and the function returned "TODO" before any of its three topology heuristics ran. The onboarding then wrote a config with an undetected topology and nothing said why. | `sitemap_locs` plus `urlsplit(u).path`, which handles both forms. | `tests/test_lib_html.py::test_a_site_relative_loc_is_returned_as_written`. |
| B-081 | 2026-09-13 | 2026-09-13 | `web/app/api/auth/google/callback/route.ts`, `save-token/route.ts` | **The Google access tokens were readable by any script on the page.** `gsc_access_token` and `gbp_secondary_access_token` were written with `httpOnly: false`, as were `gsc_user_email`, `gsc_connected`, `gbp_connected`, `gbp_secondary_user_email` and `gbp_secondary_connected` - seven cookies. The two token cookies are bearer credentials for a client's Search Console and Google Business Profile: with one, an attacker reads every query, click and impression the client has; with the other they can edit the client's public listing, its hours, its address and its replies to reviews. One XSS anywhere in the app, one bad dependency, or one browser extension with host permissions, and they leave. **Nothing read them from the browser** - `grep -rn 'document.cookie' app lib components` returns nothing, and the UI gets its connection state from `GET /api/auth/google/status`, a server route - so the flag was pure exposure with no feature behind it. `secure` was also unset on every one, so all seven could travel over plain HTTP. | All OAuth cookies now go through `oauthCookie(maxAge)` in `web/lib/oauthCookies.ts`, which sets `httpOnly: true`, `sameSite: lax`, `path: /` and `secure` outside development. **`httpOnly` is not a parameter** - `oauthCookie.length === 1` is asserted - because making it an argument is how one of them ends up false again. | `web/tests/oauth.test.mjs`: `no route sets an OAuth cookie outside the policy` counts `cookieStore.set(` against `oauthCookie(` in all three writing routes and fails if they diverge, plus a direct grep for `httpOnly: false`. `npm test` -> **229 passed**; `npx tsc --noEmit` clean; `npm run build` succeeds. |
| B-082 | 2026-09-13 | 2026-09-13 | `web/app/api/auth/google/route.ts`, `callback/route.ts` | **The OAuth flow had no state nonce, so any site could complete it in a victim's browser.** `state` was `` `${targetService}:::${encodeURIComponent(returnTo)}` `` - two application values and nothing unguessable. The callback parsed it and acted on it without ever asking whether it had started this flow. An attacker obtains an authorization code for their own Google account, then causes the victim's browser to hit `/api/auth/google/callback?code=<attacker's>&state=unified:::/profile`; the victim's session is now connected to the attacker's Google account, and every subsequent "connected" read - Search Console properties, GBP locations - is the attacker's data presented to the victim as their own. The reverse also holds where the victim is induced to authorize: nothing binds the callback to the browser that began the flow. | `state` is now a 256-bit nonce from `crypto.getRandomValues` and nothing else. It is stored in an httpOnly cookie with a 10-minute TTL, compared in constant time on the way back, and the comparison happens **before** the token exchange - an authorization code is single-use, so a forged callback that reaches Google's token endpoint has already burned the real user's code even if we discard the result. Service and return path moved into httpOnly cookies, so the callback reads them from us rather than from the URL it was handed. All three flow cookies are deleted on both the success and failure paths, making the nonce single-use. | `web/tests/oauth.test.mjs`: `the nonce is 256 bits of hex and never repeats` (200 draws), `a missing, short or wrong nonce never matches` (including two empty strings, which a naive `a === b` would accept), `the callback refuses before spending the authorization code` (asserts the index of `nonceMatches(` precedes `oauth2.googleapis.com/token`), `state carries the nonce and nothing an attacker can act on`, `the nonce is single-use`. |
| B-083 | 2026-09-13 | 2026-09-13 | `web/app/api/auth/google/callback/route.ts` | **Open redirect: the post-sign-in destination came out of the attacker-writable state string.** `stateReturnTo` was `decodeURIComponent(rawState.split(':::')[1])` and reached `NextResponse.redirect(`${origin}${destination}${sep}connected=...`)`. The two guards present checked only that it did not start with `/integrations/google` or `/api/auth`. `destination = "@evil.com"` yields `https://app.example.com@evil.com`, whose **host is evil.com** - userinfo, not a path. `//evil.com` and `/\\evil.com` are read as protocol-relative by browsers. So a link that genuinely signs the user into their own Google account could land them on an attacker's page, with the credibility of having just completed a real sign-in on the real domain. | `safeReturnPath()` in `web/lib/oauthCookies.ts` is the only way a destination is accepted, on both sides of the flow: rooted at exactly one slash, no backslash, no `://`, no `@`, no control characters, at most 512 bytes, never `/api/auth`. Anything else returns null and the caller falls back to its own default. The destination also no longer travels in `state` at all. | `web/tests/oauth.test.mjs::nothing that can change the host survives` enumerates `@evil.com`, `//evil.com`, `/\\evil.com`, `https://evil.com`, `javascript:alert(1)`, `/a\\b` and a CRLF injection, plus `a non-string or oversized path is refused rather than coerced`. |
| B-084 | 2026-09-13 | 2026-09-13 | `web/app/api/auth/google/save-token/route.ts` | **An unauthenticated POST could overwrite this deployment's OAuth client secret.** The route had no `authenticateRequest`, no origin check, no rate limit and no body-size limit. Its `save_credentials` action writes `google_client_id` and `google_client_secret` cookies, which `/api/auth/google` and the callback read in preference to nothing else when the env vars are unset - so anyone able to make the operator's browser POST here could point the next "Connect Google" click at their own OAuth application and receive the consent. Two lesser problems in the same handler: the token path made outbound requests to two Google endpoints with a caller-supplied `Authorization` header, and **echoed Google's response body back** on failure, which turns the route into a token-probing oracle running on our server rather than the attacker's. | Signed in (`authenticateRequest`), same origin (`sameOriginPost`, since a cookie-authenticated POST needs its own CSRF answer), rate limited to 10/minute per user, body capped at 16 KB, every field type-checked, and all cookie writes through `oauthCookie`. Google's error body is no longer echoed - the operator gets an actionable sentence keyed on the status code instead. | `web/tests/oauth.test.mjs::manual credential entry requires a signed-in operator, same origin, rate limited` asserts `authenticateRequest` appears before the first `cookieStore.set(`, plus `Google's error body is not echoed back to the caller`. |
| B-085 | 2026-09-13 | 2026-09-13 | `web/app/api/auth/google/status/route.ts`, `web/components/dashboard/GoogleServicesHub.tsx` | **The connection status screen stated two things it had not measured.** `userEmail: userEmail || gbpSecondaryEmail || "connected@google.account"` printed a fabricated address whenever Google's userinfo call had failed or been skipped - and it reads exactly like a real account to the operator deciding whether the right Google account is connected. `hasLocations: true, // Auto-probed` was returned unconditionally, having probed nothing, so the Business Profile card reported locations on an account that may have none. Same family as B-053: a component rendering a state instead of reading one. | Email is `null` when unknown. `hasLocations` is `null` for "nothing has asked", and the consumer's type widened to `boolean \| null` so the distinction survives to the UI. The 401 branch also now says the connection expired and to reconnect, rather than `Status 401`. | `web/tests/oauth.test.mjs::an account with no email shows no email` and `locations are not claimed by a route that never asked`. Both greps run over the source with comments stripped, because the file now documents the strings it no longer contains and a raw grep matches the explanation. `npx tsc --noEmit` clean. |
| B-080 | 2026-09-13 | 2026-09-13 | `pipeline/gates/robots_aicrawler_check.py` (`_agent_matches`, `rules_for_ua`) | **A training-crawler opt-out was reported as blocking its citation sibling, failing the PR.** `_agent_matches` compared the robots.txt token to the crawler name in BOTH directions (`ua.startswith(agent) or agent.startswith(ua)`) plus a bare substring test (`agent in ua`). So `User-agent: Applebot-Extended` / `Disallow: /` - the exact block Apple documents for opting out of model training - matched the CITATION crawler `Applebot`, and the gate went RED. A client doing the completely ordinary thing (allow everything, opt out of training) had their PR failed by the gate that exists to protect their AEO, for a robots.txt that was correct. Blocking training crawlers is documented in that same module as a legitimate choice that never gates, so the gate contradicted its own doctrine. The substring arm was worse still: `User-agent: bot` would have matched every crawler in all three lists. Found by the first test file this gate has ever had. | Matching is one direction only, per RFC 9309 and Google's own matcher: the robots.txt token matches when it is a case-insensitive PREFIX of the crawler's product token. `Applebot` addresses `Applebot-Extended`; `Applebot-Extended` does not address `Applebot`. Group selection is also longest-match rather than last-match, so the verdict no longer depends on the order a generator happened to emit two groups in. | `tests/test_robots_aicrawler_check.py::test_a_training_optout_never_blocks_its_citation_sibling` (fails on the old matcher, passes on the new), plus `test_the_token_matches_forwards_only` and `test_the_longest_matching_token_wins_regardless_of_order`. Whole file 28 tests. `python -m pytest -q` -> **1210 passed**. |
| B-055 | 2026-09-12 | 2026-09-12 | `pipeline/scanner/audit.py` (`health_score`), `web/app/ReaiDashboard.tsx`, `web/components/dashboard/AuditHeroBar.tsx`, `MeasureScreen.tsx` | **One scan reported three different health numbers.** The scanner used `max(0, 100 - 10*errors - 3*warns)`; `ReaiDashboard` recomputed a pass rate; `AuditHeroBar` carried a third, `max(20, 100 - 12*errors - 4*warns)`, with different weights and a floor of 20. A live free scan of example.com scored **0** from the scanner and **33** as a pass rate, and the hero bar showed **20** because `report?.score || (...)` discarded a genuine 0 as falsy. The old model also saturated (any site with 10+ errors scored 0, so fixing 200 of 400 errors moved nothing), had no denominator (a 10-page and a 100,000-page site with equal error counts scored the same), and read high on bad pages - the existing `assemble` test had a page where **every graded check failed** and the formula called it **87**. | `audit.health_score(counts)` is the only formula now, and it is published in full: `ok / (ok + warn + error)`. Info rows are excluded from both halves, being facts rather than verdicts. Both web formulas deleted; `dynamicHealth` and the hero bar read `report.score`. `score_version` ships beside the score so a movement caused by us is distinguishable from one caused by the site. Deliberately NOT replaced with a better composite: reporting one measure rather than several measurably increases surrogation (Choi, Hecht & Tayler 2012), published weights are not importance (Becker et al. 2017), and every vendor composite here is unvalidated - Ahrefs lets you relabel an Error as a Warning to raise the score without touching the site. The per-pillar scores stay the thing to read. | `tests/test_scanner_audit.py` pins the properties the old model lacked: **monotonicity** (a loop fixing findings one at a time asserts the score never falls), **None rather than 0** when nothing gradeable ran, info rows moving nothing, no saturation (400 errors -> 0, half fixed -> 50), and the version shipping beside the score. `web/tests/nofabrication.test.mjs` fails if either web file computes a score from error counts again, or coerces `report.score` with `||`. Both guards bit while being written. Live scan after the change: **score 33, score_version 2, counts {error: 8, warn: 21, info: 8, ok: 14}** - the same scan scored 0 before. `python -m pytest -q` -> **1024 passed**; `npm test` -> **147 passed**; `npx tsc --noEmit` -> no errors. |
| B-060 | 2026-09-12 | 2026-09-12 | `pipeline/scanner/crawl.py`, `pipeline/scanner/server.py` (`build_report`) | **The site-wide crawler was complete, tested, and called by nothing, so no scan ever produced a site-level finding.** `crawl_site` and `site_rows` detect broken internal links, orphan pages, duplicate titles and duplicate meta descriptions across a crawl. `grep -rn 'site_rows\|crawl_site' pipeline tests` returned only `crawl.py` itself and `tests/test_scanner_crawl.py` - the scan used `multipage.discover_pages` instead, which fetches a flat list of URLs and can therefore only ever run PER-PAGE tools. Every site-level check the crawler already knew how to run was unreachable: a free scan reported **zero** broken internal links and zero orphan pages on any site. B-007's shape, and the second instance found today (see B-052). **A latent bug inside it surfaced on wiring:** `_norm()` appends a trailing slash so `/a` and `/a/` dedupe to one page - a canonical form for COMPARISON - and `crawl_site` was using that string as the **fetch** URL. It would have requested `/a/` from a server publishing `/a`, taking a redirect at best and a 404 at worst, then reporting a working page as broken. Invisible while nothing called it, and its own fixtures happened to use trailing slashes throughout. | `build_report` now walks with `crawl_site` and feeds the result to both consumers: `site_rows` for the site-level group, and the per-page tools for the pages it found. The normalised string is the dedup key; the URL as published is what gets fetched and reported, for links and sitemap `<loc>` entries alike. The scan's `fetch` returns a 4-tuple, so it is adapted at the call site rather than widening the crawler's own 2-tuple contract, which its standalone tests depend on. | Live scan of `https://www.iana.org/help/example-domains` with `crawl_pages=8`: **8 pages walked, 2 site-level rows** (`site.pages_crawled` "8 pages", `site.orphan_check` "0 unreached") where the same scan previously produced **0**. Both codes are claimed by the Crawl Issues view, verified programmatically. 59 rows total. `python -m pytest -q` -> **1018 passed**; `npm test` -> **145 passed**. |
| B-057 | 2026-09-12 | 2026-09-12 | `web/app/api/content/generate/route.ts` | **The content tools demanded an `ANTHROPIC_API_KEY` this product does not use.** The route called `@anthropic-ai/sdk` and returned a 501 - *"Content tools need ANTHROPIC_API_KEY in the web environment"* - unless a separately-billed API key was configured. Every other model call in the repo shells out to the `claude` CLI (`pipeline/audit/remediate.py:324`, `pipeline/audit/seed_queries.py:235`), which runs on the operator's Claude subscription. Drafting was the only lane asking for its own metered account, so all five content tools were dead on a machine that had the subscription and no API key - which is every machine here. Found when the operator said, with feeling, that this product uses the Claude subscription. | The route spawns `claude -p --model sonnet --allowedTools ""` with the prompt on STDIN, matching both existing call sites including the reason the prompt goes on STDIN rather than argv (a leading `---` reads as a malformed flag). `--allowedTools ""` because drafting returns text and must not touch the filesystem - editing a repo is remediation's lane, under a tier, with the gates watching. 3-minute timeout, `cancel()` kills the child on client disconnect, and ENOENT reports that `claude` is not on PATH and that this product drafts through the subscription. The `@anthropic-ai/sdk` dependency is removed; nothing imports it. | `which claude` -> `/Users/both/.local/bin/claude` (2.1.175). The exact invocation verified standalone: `printf '...' | claude -p --model sonnet --allowedTools ""` -> `PIPELINE OK`, exit 0. End to end through HTTP with 51 findings from a live free scan: `POST /api/content/generate` -> **200 in 21s**, an answer-first rewrite with Title Case headings, a `[confirm: ...]` placeholder instead of an invented owner, and a table naming each AEO finding and what it did. `npm test` -> **145 passed**; `npx tsc --noEmit` -> no errors. |
| B-056 | 2026-09-12 | 2026-09-12 | operational - `pipeline/scanner/server.py` on 127.0.0.1:8765 | **The dashboard was served by an 18-hour-old scanner process, so none of this session's scanner fixes were live.** A real scan through the running backend still returned pass rows with `code: ""` and the pre-rename labels "AI crawlers allowed" / "AI training crawlers allowed" - i.e. B-047 and B-049 looked unfixed from the UI while both were fixed on disk and green in pytest. Python has no hot reload; `next dev` does, which is why the web-side fixes appeared immediately and the scanner-side ones did not. Found while running a live free scan to answer whether the Overview surfaces real data. | Restarted the scanner (`python -m pipeline.scanner.server`). Not a code defect - recorded because the failure mode is invisible: the UI shows stale scanner behaviour with a green test suite, and the natural conclusion is that the fix did not work. **Editing `pipeline/` requires a scanner restart; editing `web/` does not.** | Re-ran the same free scan against the restarted backend: **0 codeless rows** (was 5) and AEO labels now `['AI citation crawlers', 'AI training crawlers', 'Business schema', 'Answer-engine schema', 'Answer-first structure', 'Statistics and data', 'Quotes and citations', 'Data tables']`. Fed the real report through the dashboard's own derivations: 5 of 6 pillar cards measured, journey correctly at Stage 2 of 6, 6 priorities, and **7 report views populated - including Technical Checks (12 rows) and Content & Trust (12 rows), the two screens added this session whose rows previously reached no screen.** |
| B-053 | 2026-09-12 | 2026-09-12 | `web/app/ReaiDashboard.tsx` (Overview KPI strip, traffic chart, `resolveProjectData`) | **The Overview screen stated eighteen figures it had never measured.** Headless render of `/dashboard` with no client and no scan showed `+14.2%` growth, `$2,439/mo` traffic value, `Top 35%` authority, **`Grade B+` beside a 0% score**, `609 pages checked`, `609 URLs checked`, `85%` AI readiness, `~342 brand mentions` twice, four per-engine scores in bar tooltips, `All Tools (156)`, a nav badge `160`, and `7 AT RANK #1` next to `0 Ranked Keywords`. Three were client-specific inventions: a **`4.8\u2605 / 128` rating and "Hospital & Clinic" category chosen by `currentDomain.includes("hospital")**" (a rating and review count with no provenance - the exact claim `CLAUDE.md` forbids, and B-043's shape); **four named Cambodian hospitals handed to any `.kh` domain as its competitors**, with invented `-leader.com` domains for everyone else; and **configured keywords given position 3/7/14 and volume 600/520/440** when no scan had run. Plus a `Math.max(120, ...)` visits floor and constant `pagesPerVisit`/`avgDuration`/`bounceRate`. **Why every earlier sweep missed it:** `resolveProjectData` was already clean (`authorityScore = 0`, `refDelta = ""`, with comments); the constants were printed by the JSX on top of those honest values. The `Grade B+` badge computed its COLOUR from the real score and kept the letter frozen. | Every figure with a real source now reads it: tool counts from the catalog, pages from `site.pages_crawled`, AI readiness from the canonical `derivePillars` AEO pillar, the grade from the score beside it. The rest say "Not measured". Competitor and keyword fallbacks deleted outright - absent is absent. | `web/tests/nofabrication.test.mjs` (5 tests) pins the literals, the competitor fallbacks, the rank/volume ladders, the domain-substring conditional, the chart fixtures and the visits floor; it strips comments first so the file can still document what it used to print. **The test found two defects I had missed** (the star rating and the GBP category). `npm test` -> **132 passed**; `npx tsc --noEmit` -> no errors; `python -m pytest -q` -> **1005 passed**. Re-rendered headless after the fix: `STILL FABRICATED: NONE`, and the cards read "Organic Visits 0 / Authority Score Not measured / Site Health Not measured / AI Readiness Not measured". |
| B-051 | 2026-09-12 | 2026-09-12 | `web/components/dashboard/ProjectJourney.tsx`, `web/app/ReaiDashboard.tsx:4538`, `web/components/dashboard/Overview.tsx:41` | **The six-stage Project Journey bar reported a stage nobody had reached.** Both call sites hardcoded `currentStep={4}`; `Overview` additionally passed `hasClient={true} hasScan={true}`. An empty account - no client, no scan - rendered **"Stage 4 of 6: Review Top Priorities"** with steps 1-3 ticked green. `ReaiDashboard` passed real `hasClient`/`hasScan` but still pinned the stage and never passed `hasGsc`, so Search Console read as disconnected even when connected. Steps 5 and 6 could never become current, step 6 never completed, and steps 1, 4 and 6 were click targets that did nothing. Same family as the deleted 160-tool catalog and the fabricated "Resolved" remediations: a component rendering a state instead of reading one. Found when the operator said the per-client fix flow did not exist; it did, as decoration. | New `web/lib/journey.ts` owns the stages and the rules. `deriveJourney(evidence)` returns per-step status; the component takes the same evidence, so no stage number survives to be hardcoded. A stage completes only on its own artifact (client row, connected GSC property, scan in memory **or** recorded on the client row, real findings, a remediation that ran, and a second scan for the trend step - one point is not a direction). `current` is the lowest **incomplete** step rather than the furthest reached, because the stages are not a strict sequence and a bar that marched past a skipped step would stop asking for it. | `web/tests/journey.test.mjs` - 11 tests, mostly about what must not be claimed: an empty account claims nothing, malformed input does not throw, a clean scan gives nothing to triage, one scan is not a trend. `navigation.test.mjs` now asserts the exported `JOURNEY_STEPS` instead of grepping the component for label text (a grep would have passed against a comment), plus a new guard that fails if any caller reintroduces a literal stage or `hasClient={true}` - confirmed to bite, it caught a comment quoting the old prop. `npm test` -> **126 passed** (was 114); `npx tsc --noEmit` -> no errors; `python -m pytest -q` -> **1005 passed**. |
| B-049 | 2026-09-12 | 2026-09-12 | `pipeline/scanner/audit.py` (`aeo_rows`), `pipeline/scanner/checks.py` (`STATIC_CHECKS`) | **One check answered to two names depending on whether it passed.** The citation-crawler and training-crawler checks emitted `what: "AI crawlers allowed"` / `"AI training crawlers allowed"` on the pass branch and `"AI crawlers blocked"` / `"AI training crawlers blocked"` on the fail branch, because the two outcomes were two separately hand-written dicts. Consequences: anything grouping by label saw two checks where there is one; `rowsForView` de-duplicates on code plus `what`, so one check could occupy two rows; and the 115-check catalog in `checks.py` could only ever name one of the two, which is the half a client sees advertised. Found during the thermo-nuclear review of the pass-row change, when collapsing the branches made the two names collide. | `_check(code, label, failures, pass_why)` builds both outcomes from one code and one name - `failures` is the detail per failing instance, so the zero-or-one checks and the one-row-per-blocked-crawler check are the same shape. Labels are now neutral ("AI citation crawlers", "AI training crawlers"): the name says what is checked, the severity says how it went. `_row` gained an optional `what` so a named check keeps its name on the failing row too. | `tests/test_scanner_checks.py::test_fixed_label_tools_really_emit_their_catalog_labels` is what caught the mismatch and now passes against the neutral names - it asserts every catalog label is really emitted by its tool, so the catalog cannot drift from the rows again. `python -m pytest -q` -> **1005 passed**; `npm test` -> **114 passed**; `npx tsc --noEmit` -> no errors. |
| B-047 | 2026-09-12 | 2026-09-12 | `pipeline/scanner/audit.py` (`_pass_row`), `pipeline/scanner/plan.py` (`build_plan`) | **Every passing check was emitted with `code: ""`, so no passing check reached any screen.** `rowsForView` matches rows by code and skips codeless ones, so a site where every AI check passed rendered an **empty** "Crawler Findings" screen showing the hint *"Run a scan. The AI visibility check is free and runs by default."* - a check that ran and passed was indistinguishable from a check that never ran. Same family as `CLAUDE.md` known-edge 4 (*a gate that scanned nothing must never report a pass*), inverted: here a check that scanned and passed reported nothing. Seven call sites in `audit.py`: the whole SEO checklist pass branch plus five AEO passes. | `_pass_row(code, label, why)` now carries the code of the finding it is the absence of. A pass is never actionable (`severity: "ok"`), so `plan.py` cannot turn it into a work item. **Consequence fixed in the same commit:** `RESOLVED` was `prev actionable and code not in current`; a coded pass row put the code back in `current`, which would have dropped fixed findings out of both the worklist and the resolved list. The rule is now *last time actionable, this time not actionable*, which is also stronger - an explicit `ok` row is positive proof of a fix, where absence could equally mean the tool did not run. | `python -m pytest tests/test_scanner_audit.py tests/test_scanner_plan.py -q` -> **34 passed**, including `test_aeo_clean_robots_reports_the_crawler_checks_as_passing` (asserts the code is present with severity `ok` and `fix == "passing"`) and `test_a_passing_row_resolves_last_month_s_finding`. Five existing audit tests asserted *code absent* to mean *check passed*; they now assert severity, which is the precise claim. Full suite `python -m pytest -q` -> **1001 passed**. |
| B-046 | 2026-09-12 | 2026-09-12 | `web/lib/reportViews.ts`, `web/app/ReaiDashboard.tsx` (NAV_SECTIONS) | **Eleven code families the scanner emits on every run reached no sectioned screen.** The AEO strengthening pass (7f8ca2b) added `aeo.training_crawler_blocked`, `aeo.answer_schema_missing` and `aeo.article_author_missing`, and the three AI findings screens showed none of them. The same hole covered `dfs.op.*` (the DataForSEO per-page flags), `site.*` (the free multi-page crawl), and the whole free technical and content lane - `tech.` `schema.` `valid.` `content.` `eeat.` `video.`. **Root cause:** `reportViews.test.mjs` asserted every view code is emitted by the scanner and nothing asserted the reverse, so a new code was measured on every run and displayed nowhere. This is B-007's shape (*implemented is not wired*) in the view layer. | Codes routed to the screen whose question they answer: the three AEO codes to Answer Readiness / Crawler Findings / Citation Signals, `dfs.op.` and `site.` onto Crawl Issues (both crawls, free and paid, report there now). Two new views with nav entries for the lanes that had none: **Technical Checks** (`tech.` `schema.` `valid.`) and **Content & Trust** (`content.` `eeat.` `video.`). | `web/tests/reportViews.test.mjs` gains *"every code the scanner emits reaches a screen"* - every family is claimed by a view or listed in `UNSECTIONED` with a reason - and *"the family list covers every prefix the scanner stamps"*, so a new family forces that decision. **Confirmed to bite:** deleting `aeo.training_crawler_blocked` from the view fails it with `these codes are measured on every scan and appear on no screen: aeo.training_crawler_blocked`. The forward test was also repaired: it read seven modules and matched codes only as literals, so `make_row("tech")` read as unemitted. `npm test` -> **114 passed**, `npx tsc --noEmit` -> no errors. |
| B-045 | 2026-09-12 | 2026-09-12 | `web/app/ReaiDashboard.tsx:10103` | **The tools directory still advertised 156 tools.** a7dafbe deleted the fabricated 160-tool catalog and wired the directory to the scanner's real 23 tools / 117 checks, but the search box's placeholder kept a hardcoded count: the input read *"Search across all 156 tools"* while the badge one line below rendered the true `{catalogSummary.tools}`. The screen contradicted itself. | The placeholder is now a template literal over `catalogSummary.tools`, the same source the badge uses, so no count can be typed into a string again. | `grep -n '156' web/app/ReaiDashboard.tsx` -> no hits. `npx tsc --noEmit` -> no errors. |
| B-044 | 2026-09-12 | 2026-09-12 | `web/lib/reportViews.ts` (the `domain-overview` view) | **Domain Overview told the operator its own tool was not in the scan.** Its empty hint read *"The Domain Overview tool is not part of the scan yet"*, which is false: `dataforseo.keywords_card()` calls `domain_overview()` on every paid run (`pipeline/scanner/dataforseo.py:240`) and `parse_domain_overview` stamps `dfs.domain_overview` (`:195`). A doc-shaped claim inside code, stale exactly the way `CLAUDE.md` §3 describes. | Hint repointed to what actually populates it: *"Run a scan with the Keywords tool enabled; the domain overview comes with it."* | `grep -n 'def domain_overview' pipeline/scanner/dataforseo.py` -> `:201`, called at `:240` inside `keywords_card`. `npm test` -> **114 passed**. |
| B-042 | 2026-09-08 | 2026-09-08 | `pipeline/seed/posters.py` (`_http_post_devto`) | **Dev.to's Cloudflare 403s the default `Python-urllib/x` User-Agent, so the first real green poster could never post.** First live `wf-seed --live` returned `HTTP 403 from https://dev.to/api/articles` with a valid key — draft generated fine (`dropped=0`), the POST alone was rejected. Isolated against the live API: authed `GET /articles/me` → 200, but `urllib` POST → 403, while an identical `curl` POST (same key, same body, incl. `canonical_url`) → 201. The only material difference was the UA header; Dev.to sits behind Cloudflare, which bot-blocks the stdlib default. **Broader risk noted:** `pipeline/audit/providers.py:_request` also uses `urllib` with no custom UA — any Cloudflare-fronted provider it calls could hit the same 403 (not observed in the B-041 live run, so left as-is, flagged here). | Added `"User-Agent": "seo_agent-seed/1.0"` to the POST headers. | Live re-run after the fix: `DEVTO_API_KEY=… wf-seed --gaps /tmp/devto-gap.json --live` → `posted=1 queued=0 skipped=0 dropped=0`, `seed-log.json` carries the real draft URL `dev.to/…/how-to-pick-a-widget-for-remote-teams-…` with `detail: "draft (unpublished)"`. Offline suite: `.venv/bin/python -m pytest tests/test_seed_posters.py -q` → **12 passed**. |
| B-043 | 2026-09-09 | 2026-09-09 | `.github/workflows/seo-health.reusable.yml:252-255` (the four tag assertions in **Assert live SEO health**) | **A green page reported as missing every SEO tag, because `grep -q` succeeded.** The assertions ran as `printf '%s' "$body" | grep -qi PAT` under the step's `set -uo pipefail`. `grep -q` exits at the first match; the writer then dies of SIGPIPE (bash 5 prints `printf: write error: Broken pipe`) and **pipefail returns the writer's non-zero as the pipeline's status**, so `|| { ... fail=1; }` fires exactly when the tag IS found. Racy on body size, so a different subset of routes false-failed each night — it read as a flaky client site, not a broken check. `lee-wave/lee-series-web` ran red on the daily `schedule:` for a week (runs #26-#31, 2026-09-04 to 2026-09-08) and the noise was attributed to the site. **Only `seo-health.reusable.yml` matched `\| grep -q` in the repo, so that is the whole blast radius.** | Live pages proven clean first: `curl -sL https://www.leeserie.com{/,/product,/about-us,/contact-us}` → all four tags present on all four routes, against run 34253833350 reporting eight of them missing. Then reproduced on the runner's shell over lee's real bodies — `docker run --rm -v "$PWD:/w" bash:5 bash /w/repro.sh` → `RESULT old=8 new=0`, the same eight the production run reported. (macOS bash 3.2 does **not** exhibit it: `PIPESTATUS=0 0`. Local testing was never going to catch this one.) Fixed by dropping the pipe: `grep -qi PAT <<<"$body"`. Guarded by `tests/test_no_sigpipe_grep.py`, confirmed red against the pre-fix file (`git stash` → 1 failed) and green against the fix (4 passed, 2 skipped). Shipped in **v3.1.4**. |
| B-041 | 2026-08-14 | 2026-08-18 | `pipeline/audit/providers.py:118-148` (`crux_findings`) | **CrUX queried the literal config domain, not the host Chrome actually recorded traffic against.** Any client whose apex 301s to `www` (or the reverse) risks an exact-origin miss: CrUX's dataset is keyed per exact origin and is not redirect-aware. Proven live 2026-08-14 while verifying all three optional providers for the first time against real credentials: `wikipedia.org` (bare) returned no CrUX record; `en.wikipedia.org` (the real serving origin, same site) returned a real one. `new-wave.io` returned no record under either its bare or `www` form — consistent with too little Chrome traffic either way, not proof either origin was queried correctly, which is what motivated checking the mechanism directly against a known-high-traffic site. | Origin-level queries now resolve the domain's real serving host via the existing `curl_final_host` helper (built for B-037) before building the CrUX request, trusting the result only when it is the same domain or a subdomain of it — a thermo-nuclear review of the fix caught that a naive substitution would have let an auth wall's own domain (curl_final_host's original B-037 case, e.g. `vercel.com`) resolve through and report a stranger's real Core Web Vitals as the client's. Falls back to the literal domain if resolution fails or fails the same-site check. Per-URL queries are untouched. | `tests/test_providers.py::test_crux_queries_the_resolved_origin_not_the_configured_domain` and five siblings assert the resolved host reaches the request, an unresolvable domain falls back safely, a resolved-but-different-site host (the auth-wall case) is not trusted, per-URL mode never calls the resolver, and the "no field data" message names the host actually queried. `.venv/bin/python -m pytest -q` → **714 passed**. |
| B-040 | 2026-08-11 | 2026-08-11 | `docs/gate-reference.md:5` (the **Authority** line) | **`gate-reference.md` cited three documents as its authority and none of them exist.** The line named `PIPELINE-MASTER-BUILD-PLAN.md#exit-code-registry-keep-gates-attributable` for the exit-code registry, `VERIFY-REPORT-RUN1.md` for real-world gate verdicts (*"full suite run against a real Acme Next 16 build, 103 HTML files / 99 sitemap URLs, 2026-07-19"*), and `DOCTRINE-GATE-MATRIX.md` for doctrine provenance. `ls docs/` returns none of the three; they went with an earlier cleanup and the reference was never repointed. This is the same class as the 2026-08-06 `MODULES.md` defect CLAUDE.md §3 cites — a doc whose stated source of truth cannot be opened is worse than one with no citation, because it reads as verified. It also matters more here than elsewhere: this file is what an operator reads to find out whether a gate blocks and what its exit code means, and the **WORKS?** column throughout is populated from the run report that is gone. Found by opening the three links while updating the file for B-038/B-039. | Authority repointed to what actually exists and is actually consulted at runtime: the exit-code registry in the header of `.github/workflows/quality-gate.reusable.yml` plus the `reg()` case block in its Evaluate step (those two are what CI attributes a failure to), and each gate's own module docstring in `pipeline/gates/`. The dead links are recorded in place rather than silently deleted, with the consequence spelled out — the **WORKS?** verdicts are dated observations whose run report no longer exists, so they mean *last seen working*, not *currently proven*. | `ls docs/PIPELINE-MASTER-BUILD-PLAN.md docs/VERIFY-REPORT-RUN1.md docs/DOCTRINE-GATE-MATRIX.md` → `No such file or directory` ×3. `grep -rn` across the repo confirms `gate-reference.md:5` was the **only** surviving reference to any of them, so nothing else needed repointing. Docs-only; direct to `main` per CLAUDE.md §5. |
| B-039 | 2026-08-11 | 2026-08-11 | `.github/workflows/quality-gate.reusable.yml` (the `for g in …` evaluate loop) | **B-038's fix changed the words and not the verdict — it shipped in v3.1.2 and did not work.** The evaluate step has **two** lists over the same gate names: `add_fail` builds the sticky PR comment, and a separate `for g in TSC SSR … BUILD … ACCEPTANCE` loop reads `"${!g}"`, emits the `::error::` annotation, sets `red=1` and exits 1. **Only the loop decides the verdict.** B-038 rewired `add_fail` and left the loop alone, so the run still failed on `steps.build.outcome` exactly as before while the comment claimed otherwise. Caught on the real client one release later: run **31471589695** on `lee-wave/lee-series-web`, pinned to the freshly cut `@v3.1.2`, still annotated `##[error]Blocking gate failed: BUILD (exit 1 : framework build produced no output dir)`. Invisible to the tests that existed because the loop reads the variable **indirectly** — the literal string `[ "$BUILD" = "failure" ]` really was gone, which is what `::test_a_crawled_tree_does_not_block_the_pr_on_the_failed_build` asserted, so the assertion passed over a broken workflow. **This is `CLAUDE.md` §4's "implemented is not wired" (B-007) one layer further in:** the call site is a bash word inside a loop list, which no unit test of any Python module can reach, and the tempting text assertion matches the half that does not decide anything. | `BUILD` removed from the loop's gate list, and the tree question checked once, before the loop, for both halves of the report: `if [ "$TREE" != "true" ]; then echo "::error::No HTML to judge …"; red=1; fi`. A comment at the loop names it as the thing that fails the run, so the next person editing `add_fail` knows it is not enough. `reg()`'s BUILD entry is left in place — it still labels the summary table row. | `tests/test_ratchet_wiring.py::test_the_loop_that_actually_fails_the_run_agrees_with_the_comment` **parses the loop's gate list** rather than grepping for a string, asserts `BUILD` is absent from it, and asserts six gates that must stay (`TSC SSR TIER PROVENANCE FORBIDDEN ACCEPTANCE`) are still present, so it cannot be satisfied by emptying the loop. Confirmed to bite: re-inserting `BUILD` into the list makes the parsed names contain `BUILD` again → **True**. **691 passed.** ⚠️ Same caveat as B-038 — the green-on-a-crawled-tree path still has not been observed on a real run, because lee cannot reach it until deployment protection is off (B-037). What IS now proven is the negative: the blocking loop no longer contains the term that made it unreachable. |
| B-038 | 2026-08-11 | 2026-08-11 | `.github/workflows/quality-gate.reusable.yml:569` (the blocking list) | **`render_url` could never produce a green PR for the client it exists for.** The OUT gates were correctly rewired to key on `steps.tree.outputs.ready`, so a crawled tree is judged like a built one — that half works, and `tests/test_ratchet_wiring.py` already asserted it. The **blocking** list was not rewired: `[ "$BUILD" = "failure" ] && add_fail "Build failed …"` reads `steps.build.outcome` directly. A client with no static export configures the build to fail **on purpose** (`build_command: ""`, `build_output_dir: "./out"`, and `./out` is never produced — that is the documented safe state, sharp edge #4), then supplies `render_url` so the crawl provides the tree. Such a client would run all nine OUT gates against the crawled tree, pass every one, **and still be reported RED on BUILD** — while the summary table twenty lines above printed `\| BUILD \| **crawled** <url> -> ./out \| 19 \| ✅ \|` in the same sticky comment. The two halves of one comment contradicting each other. The message was wrong too: *"All page checks below were skipped"* is false in exactly the case it fires on after a successful crawl — they all ran. Found by reading the blocking list while diagnosing why lee's PR #36 was red on BUILD; lee has not reached this state yet only because B-037 (deployment protection) stops the crawl earlier. | Block on `steps.tree.outputs.ready`, which is already computed and is the question that actually matters — *is there HTML to judge, however it got there*. Exported to the summary step as `TREE`, and the line becomes `[ "$TREE" != "true" ] && add_fail "No HTML to judge — the build produced no output dir and no render_url crawl supplied one. Every page check below was SKIPPED, not passed."` Strictly better in all four states: build succeeds → unchanged; build fails with no crawl → still blocked, with a truer message; build fails and crawl succeeds → **no longer blocked**, which is the fix; build fails and crawl fails → still blocked. The suite still cannot report a pass over a tree it never got, which is the property sharp edge #4 is about. | Asserted as workflow text, in the file the defect lived in and nowhere else — the same discipline B-007 forced: `tests/test_ratchet_wiring.py::test_a_crawled_tree_does_not_block_the_pr_on_the_failed_build` asserts the `$BUILD` test is **gone**, that `$TREE` blocks in its place, and that `TREE:` is actually exported to the step that reads it (implemented-is-not-wired, one layer down). `::test_the_no_html_message_does_not_claim_the_page_checks_ran` pins the message honest. Both fail against the previous file. **690 passed.** ⚠️ **Not yet observed on a real green run** — lee cannot reach the crawl path until B-037's deployment protection is turned off, which is an operator action in Vercel. This is asserted as text, exactly like the rest of the render-source wiring, and B-017 remains open for the same reason. |
| B-037 | 2026-08-11 | 2026-08-11 | `pipeline/audit/snapshot.py` `snapshot` + `pipeline/lib/common.py` (new `curl_final_host`) | **`wf-render-snapshot` would have crawled an auth wall's login page into the build tree and reported success — the nine OUT gates would then have judged a login screen as the client's site.** `curl` and `curl_status` both follow redirects (`-sL` / `-sIL`), which is deliberate and correct: a site with a no-trailing-slash policy needs it. An auth wall exploits exactly that. Vercel Deployment Protection answers **every** route with a 302 to `vercel.com/sso-api` and a 200 login page; Cloudflare Access and Netlify password protection do the same to their own domains. So every route returns 200, every body is non-empty HTML, and the crawler's only refusal — *"no page was fetched"* — never fires. **Found live, and it is why lee's `render_url` produced nothing:** every deployment on `lee-wave/lee-series-web` since 2026-08-11T03:11Z reports `state=failure`, `description="Deployment was blocked"`, while `curl -sIL` on the preview returns **200** and the body is `<title>Login – Vercel</title>`. Measured: `curl -s -o /dev/null -w "%{http_code}" -L https://lee-series-rn75is60j-lee-serie.vercel.app` → `200`; without `-L` → `302` to `https://vercel.com/sso-api?url=…`. **This is strictly worse than the empty tree the module already refuses.** Sharp edge #4's dangerous case is "green over nothing"; this is "green over the wrong thing", and it leaves a full, plausible-looking `./out` with no empty directory to notice. Nothing shipped a bad verdict — lee's caller resolves the preview from the GitHub Deployments API and requires `state=="success"`, so the walled deployment was correctly refused upstream and the nine gates stayed SKIPPED. The hazard is any path that reaches `wf-render-snapshot` with a URL directly. | New `common.curl_final_host(url)` returns the host a URL actually lands on after redirects (`curl -w %{url_effective}`), or `""` when unreachable. `snapshot()` probes the first route **before writing anything** and raises `SnapshotError` (exit 19, the existing refusal code) when the landed host differs from the host asked for. Compared by **host**, not by URL, so a same-host redirect — trailing-slash policy, locale prefix, the reason `-L` is there — is untouched. An unreachable probe returns `""` and falls through to the existing empty-crawl refusal, which has its own message; conflating the two would send the operator to the wrong setting. The message names the three products by name and both remedies (turn protection off, or supply a bypass secret). | **Live, against the actual walled preview:** `wf-render-snapshot --project ~/clients/lee-series-web --base-url https://lee-series-rn75is60j-lee-serie.vercel.app --out ./wallout --clean` → `[REFUSED] … redirects to a different host (vercel.com). That is an auth wall or an interstitial, not the site …` and **no `wallout/` directory was created**. Hermetic tests in `tests/test_snapshot.py`: `::test_an_auth_wall_refuses_instead_of_crawling_the_login_page` (asserts the refusal AND that the out dir is empty, with the wall answering 200 for every route so the old empty-crawl guard cannot be what fires), `::test_a_same_host_redirect_is_still_followed` (narrowed, not disarmed — the crawl still completes), `::test_an_unreachable_probe_does_not_masquerade_as_a_wall` (an unreachable host still gets the empty-crawl message). The `served` fixture now stubs `curl_final_host` too, which also removes a 30-second real network call the other 13 tests would otherwise have made. **688 passed.** |
| B-036 | 2026-08-11 | 2026-08-11 | `pipeline/gates/audit_ssr.py` `scan_file` + `_is_func_open` | **`audit_ssr` reported 6 violations on the first real client it was ever pointed at, and 5 of the 6 were false positives.** B-027 fixed the gate's *reach* (it looked only in `src/`, which `create-next-app` does not create). This is what it found once it could see: on `lee-series-web`, `[FAIL] 4 files have SSR-dangerous patterns` — every one of them code that cannot run at server render. Two independent defects, both in the "is this access actually reachable" half, and both make a **blocking, never-baselineable** gate red for no defect. **(a) The early-return guard has never once matched.** `_mask` blanks string contents before anything is scanned, so `if (typeof window === "undefined") return …` masks to `if (typeof window ===            ) return …` — and `EARLY_RETURN_GUARD` requires the quoted `'undefined'` literal that masking just deleted. The gate's own docstring documents early-return guards as an SSR-SAFE case; the code could not honour it. Proven directly: `EARLY_RETURN_GUARD.search(masked)` → `False`, `.search(raw)` → `True`. This produced 4 of the 6: `components/checkout/PaywayCheckout.tsx` L37/L42/L50 — inside a function whose **first line** is that guard — and `components/checkout/AddressPopup.tsx` L229, one line below `if (typeof document === "undefined") return null;`. **(b) A multi-line function signature opens no function frame.** `_is_func_open` only recognises a signature whose `)` sits on the same line as the `{`. `export default function AddressPanels({` … `}: Props) {` is neither: the first line has no closing paren, the second has no `function`. That is **how every React component with named props is written**, and losing the frame undercounts depth for the whole file, so `len(func_frames) < 2` reads an event handler as a render body. Produced the 5th: `components/profile/AddressPanels.tsx:181`, `window.confirm` inside `async function handleDelete(e: React.MouseEvent)` — a click handler. Confirmed: `_is_func_open("export default function AddressPanels(")` → `False`, `_is_func_open("}: Props) ")` → `False`. **The same lesson as B-027 from the other side:** a never-baselineable gate that under-scans reports a silent green, and one that over-reports makes a PR permanently red. Both end with the gate switched off. | **(a)** `EARLY_RETURN_GUARD` is matched against the **raw** line instead of the masked one. It stays honest because the caller still requires `same_line_guard` — a `typeof` on the **masked** line — so a commented-out or stringified guard proves nothing, which is asserted. **(b)** A paren-context stack carries signature state across lines: each `(` records whether a `function` keyword opened it, the matching `)` sets `sig_close`, and `_is_func_open(pre, sig_close)` accepts a `pre` ending in `)` on that basis. `sig_close` is cleared at `{` and `;` so it cannot leak. **Known ceiling, deliberately not fixed:** the depth-1 rule cannot distinguish a plain exported helper from a component render body without real parsing, so a module-level `function navigateAfterAuth() { window.location.assign(…) }` is still flagged. That is the 6th finding, and it is the one the gate is arguably right about — a browser-only helper should carry its own guard. Method-shorthand signatures spread over lines (`async handleX(` with no `function` keyword) remain unrecognised for the same reason; the failure is toward over-reporting, which is the safe direction for this gate. | **On the real client, same command, before and after:** `wf-audit-ssr ~/clients/lee-series-web` → `[FAIL] 4 files have SSR-dangerous patterns` (6 violations) → **`[FAIL] 1 files`** (1 violation, the documented ceiling above). The five that cleared are exactly the five argued above. **Narrowed, not disarmed** — three tests exist to prove the gate did not simply get quieter: `::test_a_guard_is_still_scoped_to_its_own_function` (a guard in one function does not license the next), `::test_a_commented_out_guard_does_not_count`, and `::test_a_render_body_access_is_still_caught_in_a_multi_line_component` (counting the frame correctly is what makes depth 1 still mean "runs at server render" — the blank-shell disaster this gate exists for). Plus `::test_an_early_return_typeof_guard_protects_the_rest_of_the_function`, `::test_the_guard_works_with_single_quotes_and_with_document` (masking was blind to the quote character, so both spellings and both globals are proven), `::test_an_event_handler_in_a_multi_line_component_is_not_a_render_body`, `::test_a_guarded_portal_after_a_multi_line_signature` (both defects at once), `::test_a_destructured_multi_line_signature_opens_a_function` — all in `tests/test_audit_ssr_guards.py`, all fixtures reduced from the real flagged code. Every one failed before the fix. **685 passed.** |
| B-034 | 2026-08-11 | 2026-08-11 | `pipeline/gates/claim_provenance_check.py` `build_corpus` + `is_sourced` | **`claim_provenance_check` accepted invented numbers — the gate CLAUDE.md calls the answer to "the error class models produce most fluently" was porous to exactly that error class.** Two independent holes. **(a)** A work item's `evidence` contributed its **numerals** to the corpus. Evidence is a measurement *of the page* (`len=106`, `words=478`, `count=12`) — real, which is why doctrine admits it, but never a fact *about the business*. Flattened into one corpus it sourced any claim sharing a digit string. On lee's 2026-08 cycle that is 190 evidence strings carrying 38 distinct integers, 25 of them below 200 — enough coverage of the small-number range that most invented figures land on one. **(b)** A claim with a dedicated config home was matched against the **whole** config, so `option_full_threshold_pages: 10`, an architectural escalation threshold, sourced *"Trusted for over 10 years"* for a client whose `years_in_business` is blank. Found while auditing what the gate actually refuses on the live lee PR rather than reading its docstring. Reproduce, on the real client with the gate as shipped at v3.0.0: a diff adding `"106% more hydration, 478 customers served, from $12."` to `lib/constants.ts` → `[OK] claim-provenance: every claim in 1 changed file(s) resolves to a source.` **exit 0**. Not one of the three numbers is a fact about the business. | **(a)** Evidence contributes words but not numerals — digits are substituted to `#` (not deleted, so `len=106` cannot collapse to `len=` and abut the next token's digits). The superlative check, which is textual, is unaffected; the `[CORPUS]` line now reads `worklist.json (words only, digits redacted)` so the narrowing is visible to the operator. **(b)** `SCOPED_KINDS` binds `rating`/`reviews`/`years`/`license` to `trust_signals.rating`/`.reviews`/`.years_in_business`/`.licenses` and nothing else; a blank, zero, empty-list or `<x.x>` placeholder field yields **no source**, so the kind is refused outright. `warranty` — the fifth kind CLAUDE.md names — has **no config field anywhere in the schema**, so it stays on the general corpus; scoping it to a key that does not exist would refuse every warranty term unconditionally, which is a product decision and a human's. The **prior version of the file is still checked without the scope**: a claim already published was not invented by this diff, and refusing inherited copy on every reflow is how a gate gets switched off. | Real gate, real client, same diff, before and after. **Before** (v3.0.0 gate, restored via `git stash`): `[CORPUS] 148 words from: docs/client-config.yml, docs/audit/2026-08/worklist.json` → `[OK] … every claim … resolves to a source.` **exit 0**. **After**: `[CORPUS] 148 words from: docs/client-config.yml, docs/audit/2026-08/worklist.json (words only, digits redacted)` → `[BLOCKED] 3 unsourced claim(s).` **exit 18**. On the year-count diff (`"Trusted for over 10 years, rated 4.9 stars across 1,200 reviews."`), which needed **(b)** rather than (a): before → 2 unsourced, the year-count **passed**; after → 3 unsourced, `[UNSOURCED] lib/constants.ts: 'over 10 years' (years)`. Negative control that it is narrowed and not merely stricter: `::test_a_year_count_needs_years_in_business_not_any_stray_number` asserts a client who **has** declared `years_in_business: 28` still resolves 28, while 30 is refused. Plus `::test_evidence_numerals_cannot_source_a_business_claim` and `::test_a_placeholder_is_not_a_source`, all in `tests/test_phase4_gates.py`. **677 passed.** ⚠️ **This fix does not protect lee PR #36** — that client is pinned to `@v3.0.0` and is judged by the porous gate until the tag is bumped. |
| B-022 | 2026-08-08 | 2026-08-11 | `pipeline/audit/measure.py:89` | **`health.schema_faq_missing` fires for a search feature Google retired three months ago.** Every URL without `"@type":"FAQPage"` gets the finding, unconditionally — no config key gates it. Google added the deprecation notice to the FAQ structured-data docs on **2026-05-07**; FAQ rich results stopped appearing in Search that day, the Search Console report and Rich Results Test support went in June 2026, and the Search Console API data in August 2026. `FAQPage` remains valid Schema.org and is harmless to carry, but it can no longer earn anything, so the finding is unfixable-by-value: a client with a perfect site collects one per page every cycle, it PERSISTS through the ratchet forever, and at T2 it points the agent at writing markup for a dead feature. Found while adapting the page-type shapes reference — noticed because the upstream (`AgricIDaniel/claude-seo`) had already dated the retirement, then confirmed independently against Google's own deprecation notice rather than taken from their doc. **Measured cost on a real client, 2026-08-08:** on `lee-series-web`'s 2026-08 cycle, **19 of the 93 tier-blocked work items are `schema_faq_missing`** — a fifth of everything the client cannot act on, permanently unactionable, re-filed as PERSISTING every cycle from now on. Reproduce: `python3 -c "import json;from collections import Counter;w=json.load(open('docs/audit/2026-08/worklist.json'));print(Counter(i.get('kind') for i in w['items'] if i.get('tier_blocked')))"` in the client repo → `{'h1_count_wrong': 26, 'tel_link_missing': 26, 'schema_faq_missing': 19, 'thin_content': 15, 'schema_breadcrumb_missing': 7}`. | **Fixed 2026-08-11 by deletion — option (a), the one this entry called the honest default.** `measure.py` lines 88-89 removed (the `if "FAQPage" not in types:` and its `add(...)`), leaving `schema_breadcrumb_missing` still conditional as the entry warned. Also removed: the `min_tier: 2` row in `plan.py` (it was the only T2 entry left) and the code from `AEO_CODES` in `score.py`. Doctrine in `skills/site-remediation/references/page-type-shapes.md` rewritten — the NO CHANGE instruction now scopes to old cycles whose worklist predates the deletion, since the finding can no longer be emitted. **Measured on the real client:** re-running `wf-site-health` on lee went **114 -> 95 findings**, exactly the 19 predicted, and `any('faq' in code)` is now False. AEO score rose 61 -> 72 (failing 41 -> 22, and the denominator correctly falls 104 -> 78), the correct direction: those pages were never worse for answer engines. `tests/test_measure.py::test_faq_markup_is_never_a_finding` asserts it cannot fire even on a page with no schema at all; `test_schema_findings` keeps the other two. `tests/test_score.py::test_aeo_is_never_config_gated` now asserts against `len(AEO_CODES)` rather than a literal 4, so the property survives the next change to the family. 674 passed. **Cost it caught, larger than the $9 estimated here:** on lee at T3 the finding had already been actioned once, and the 'fix' was not markup — it rendered the entire `<Faq>` accordion onto `/about-us/`, a visible design change to a client site, to satisfy a check for a feature Google switched off. That component was reverted and the changelog entry corrected from `fixed` to `no_change`. |
| B-033 | 2026-08-10 | 2026-08-10 | `pipeline/lib/common.py` `validate_profile` | **T3 could not be onboarded at all: the T2 precondition was applied to T3, blocking it at exit 5.** The check read `if tier >= 2 and not profile.get("content_location")` and raised an **ERROR**, so any full-authority client with no declared content home failed config validation and `wf-onboard` stopped at *"the config parses but does not cohere"*. T3 does not use the key: `tier_verdict` returns `True` on its `tier >= 3` branch (`common.py:400`) **before** `content_location` is read at `:409`. The error text made it worse by explaining a tier nobody asked for — a T3 operator was told *"no declared content home means **T2** is unavailable"*. Found raising `lee-series-web` to T3 on the operator's instruction. | `tier == 2`, not `tier >= 2`, and the message drops the `+`. T2 is still refused without a content home, which is the rule CLAUDE.md §Tiering actually states ("**T2** is refused without `content.location` and `content.registry`"); T3 stays governed by the deny floor alone, as designed. | Before: `wf-client-profile ~/clients/lee-series-web` → `[ERROR] tier 2+ requires content.location …`, **true exit 5**. After: exit 0, and the full run `wf-onboard lee-wave/lee-series-web www.leeserie.com --tier 3 --skip-clone` → **true exit 0**, `Tier: T3`, `worklist: 114 actionable, 0 above tier` (was `36 actionable, 78 above tier`). `tests/test_tiering.py::test_t3_does_not_need_a_content_location` asserts no errors AND that the authority is real with the floor intact (`app/(site)/page.tsx` M → True, `docs/client-config.yml` M → False). `::test_t2_without_content_location_is_fatal` still green. 673 passed. |
| B-032 | 2026-08-10 | 2026-08-10 | `pipeline/audit/onboard.py` (bootstrap call + `--tier` default) | **`wf-onboard --tier N` silently ignored the tier on every already-onboarded client and reported a clean run.** Two causes in series. First, onboard passed `--tier` but never `--add-tier`, and `bootstrap_config.main()` only consults the tier for an existing config inside `if args.add_tier:` — otherwise it prints `[OK] Config already exists` and **exits 0**. Second, `add_tier` is append-only by design: `if re.search(r"^tier:", text, re.M): print("already declares a tier"); return 0`, so it cannot rewrite a declared tier and returns success when it declines. Net effect on `lee-series-web`: `wf-onboard … --tier 3` printed `[READY]`, exit 0, while `docs/client-config.yml` still said `tier: 1` and the worklist still reported `78 above tier`. The operator had no signal at all that the request was dropped. | Onboard now passes `--add-tier` (inert on a fresh config, where `tier_block` handles the write), and a post-bootstrap guard compares the on-disk tier against an **explicitly requested** one and stops at exit 1 naming the manual fix. `--tier` defaults to `None` rather than `1` so an omitted flag stays distinguishable from `--tier 1` and ordinary re-runs of a T2/T3 client never trip the guard. Append-only `add_tier` is left as-is: a tier raise being a reviewed human commit is the model (CLAUDE.md §Tiering), and the defect was the silence, not the refusal. | Before: `--tier 3` → `[READY]`, exit 0, config unchanged at `tier: 1`. After: **true exit 1** with `[STOPPED] you asked for tier 3, but docs/client-config.yml still declares tier 1. The tier was NOT changed.` plus the hand-edit instruction, and `wf-preflight`/`wf-site-health` never run. `tests/test_onboard.py::test_an_ignored_tier_raise_stops_the_run_instead_of_reporting_ready` (asserts the stop AND that nothing downstream executed), `::test_a_re_run_without_the_flag_never_trips_the_tier_guard`, `::test_a_re_onboard_actually_applies_the_new_tier`. 673 passed. |
| B-020 | 2026-08-07 | 2026-08-07 | `pipeline/audit/remediate.py` `already_fixed` + `selectable` | **The resume set is keyed on a positional id, so any re-measure silently mis-resumes.** B-013 made `wf-site-remediate` skip what the cycle changelog records as `fixed`, keyed on the work item `id`. But ids come from `plan.py:151` as `f"wi-{cycle}-{idx:04d}"` — an **enumeration index over the sorted findings**, not an identity. Re-measure a live site, gain or lose a single finding, re-plan, and every id after that point shifts. The changelog's "fixed" ids then name different findings, so the next run skips items that were never touched while re-attempting ones that were. Hit on `lee-series-web` while re-running the 2026-08 cycle with the new SERP provider: three `serp.absent` findings entered the set and **19 of 20 fixed ids landed on unrelated work — zero stayed aligned.** `wi-2026-08-0009` was `health.title_length @ /about-us/` in the changelog and `health.schema_breadcrumb_missing @ /about-us/` in the new worklist. Caught before spending: the run was stopped at the plan step and the ids compared by fingerprint. `remediate.py:476` already said `finding_fp` "is the only exact link from a fixed item back to the finding it fixed" — and `already_fixed` keyed on `id` anyway. | `already_fixed` returns `finding_fp` values and `selectable` filters on the item's `finding_fp`. An item carrying no fingerprint is **never** skipped: attempting twice costs money, skipping a real finding and recording it fixed puts a falsehood in the artifact. | Against the real lee-series-web cycle, post-renumbering: `already_fixed` → 20 fingerprints, `selectable` → **21 actionable, 1 queued, 20 correctly skipped**. On `id` the same inputs skipped 19 items that were never fixed. `tests/test_remediate.py::test_a_remeasure_that_renumbers_ids_still_skips_what_was_fixed`, `::test_an_item_with_no_fingerprint_is_attempted_not_skipped`, plus the B-013 tests still green with fingerprints in their fixtures. Full suite `588 passed in 5.03s`. |
| B-027 | 2026-08-10 | 2026-08-10 | `pipeline/gates/audit_ssr.py:148-155` | **`audit_ssr` reports success after scanning zero files on any repo that does not use `src/`.** `candidates = [project / "src"]` plus a `project.glob("**/src")` fallback, and `if not src: print("[SKIP] No src/ directory"); sys.exit(0)`. `lee-series-web` is `nextjs-16-app-router` with its code in `app/`, `components/` and `lib/` and no `src/` anywhere, so the gate exits **0** having looked at nothing. Reproduced 2026-08-10: `wf-audit-ssr <lee>` → `[SKIP] No src/ directory in ...`, `rc=0`. This is a **never-baselineable** gate ("a pre-existing SSR crash still crashes on every request") reporting green over nothing, which `CLAUDE.md` rule 4 names as worse than a red because nobody investigates a green. | **Fixed 2026-08-10.** Option (a) and option (c) from this entry's own notes, together: the roots are derived from the repo rather than assumed, AND zero scanned files is now `CANNOT_JUDGE_EXIT = 4` rather than 0. Deliberately a DENYLIST — an allowlist keyed on `framework_family()` would scan nothing for any framework outside next/vite/wordpress, recreating this bug for the next unfamiliar client. Excludes node_modules, framework caches, build output, public/static/vendor/docs, the client's configured `build_output_dir` (via both spellings), and `*.min.js`. WordPress keeps its exit-0 skip because *not applicable* is a different claim from *cannot judge* — conflating those two is the bug. Measured: lee `[SKIP] rc=0` → `[FAIL] 4 files, rc=9` over 165 files; a JS-free repo → `[REFUSED] rc=4`. `tests/test_audit_ssr_roots.py`, 14 tests over six layouts (src/app, app/, pages/, components/, lib/, islands/). One self-inflicted bug caught by those tests: `'.output'.strip('./')` returns `'output'`, so the configured-build-dir exclusion silently skipped the wrong directory. `.venv/bin/pytest -q` → `669 passed`. **Not yet rolled out:** the gate can never be baselined, so `wf-audit-ssr` must be run against every client checkout before the tag is cut. |
| B-029 | 2026-08-10 | `pipeline/audit/remediate.py` `HUMAN_WORKLIST` + `pipeline/lib/common.py:234` `ARTIFACT_PATHS` | **The agent could write its own skip list and permanently dequeue its own work.** The standing human worklist was `docs/audit/human-worklist.md`, which is inside `ARTIFACT_PATHS = ["docs/audit/**"]` — and `tier_verdict` waves those through BEFORE it looks at the tier. Measured: `tier_verdict({'tier':1,...}, 'docs/audit/human-worklist.md', 'M')` → `(True, 'cycle artifact')`. Fix mode hands the writer `Write`, so an ordinary T1 run that wrote `<!-- fp:X -->` into that file would be recorded `fixed`, sail through `tier_check` on the PR as a routine artifact, and from the next run onward drop fingerprint X out of `selectable()` forever. B-025's own fixed entry claimed "the agent cannot suppress its own work" — true in prose, false in code, for the length of one review cycle. | **Fixed 2026-08-10, same session, found by the thermo-nuclear review before the commit landed.** Moved to `docs/human-worklist.md` and added to `DEFAULT_DENY` (with `docs/banned-phrases.txt`, see B-030). Now `(False, 'on the deny list — refused at every tier, T3 included')` for A/M/D at T1, T2 and T3. `tests/test_recommend_mode.py::test_the_agent_can_never_write_its_own_skip_list` asserts the property across all three tiers and both the constant and the floor, rather than restating the argument in a docstring. `bootstrap_config.tier_block` emits both new entries — caught by the pre-existing `test_emitted_block_parses_and_carries_the_deny_floor`, which is exactly the drift it was written for. |
| B-030 | 2026-08-10 | `pipeline/lib/common.py` `ruleset_declared_empty` | **`forbidden_phrases: []` was a fail-open the agent could trigger, because it read one of the ledger's two sources.** `forbidden_sweep` enforces the UNION of the config block and `docs/banned-phrases.txt`; the declaration only read the config, and only the config was on the deny floor. Reproduced on a T3 repo with a real hit: `exit=3 [BLOCKED] 1 forbidden-phrase hits` with the ledger present, then `rm docs/banned-phrases.txt` → `exit=0 [SKIP]`. `tier_verdict({'tier':3,...}, 'docs/banned-phrases.txt', 'D')` was `(True, 'T3 may delete')`. The docstring rested the whole safety argument on `docs/client-config.yml` being undeletable, which covered half the contract. | **Fixed 2026-08-10, same session, found by the thermo-nuclear review before the commit landed.** Two independent closures: the predicate now takes `project` and refuses the skip while a non-blank ledger exists, AND `docs/banned-phrases.txt` joined `DEFAULT_DENY` so the agent cannot produce the state at any tier. Deliberately not comment-aware — a comments-only ledger still blocks the declaration, because the lenient reading would switch a legal gate off the moment somebody commented a ledger out. Four tests in `tests/test_declared_empty_ruleset.py` cover populated-txt-beats-empty-config, the deny floor at T1/T2/T3, comments-only, and genuinely-blank. No fleet exposure: `config/client-config.starter.yml:149` and `bootstrap_config.py:353` both ship populated `forbidden_phrases:` blocks, so no existing client held the `[]` state. |
| B-018 | 2026-08-07 | 2026-08-10 | `CLAUDE.md` sharp edge #4 | **The sharp edge overstates one failure and understates another.** It says a site with no route tree "makes both gates scan nothing and report **green**". Verified against the code: `.github/actions/build-site/action.yml:189-191` exits 1 on a missing-or-empty `BUILD_DIR` and every OUT step was guarded by `if: steps.build.outcome == 'success'`, so in CI those gates were **SKIPPED**, not green. `em_dash_check` and `acceptance_check` also refuse a missing dir explicitly. Green-over-nothing is real only for a directory that EXISTS and holds no HTML — which is precisely what pointing `build_output_dir` at `.next` would produce, so the wording steers a reader toward the one actively dangerous fix. It also names two gates when **nine** read the build tree, including `forbidden_sweep` (never-baselineable, legal exposure). | **Fixed 2026-08-10**, in the same session that re-verified both halves of the claim, per the entry's own condition. Sharp edge #4 now says **nine** gates rather than two, separates skipped-on-an-absent-tree from green-over-an-empty-one, names `build_output_dir: .next` as the way to produce the dangerous variant, and points at `wf-render-snapshot` as the supported answer. Re-verified rather than copied: `wf-audit-ssr <lee>` → `[SKIP] ... rc=0` (which is how a gate scanning nothing looks), and `wf-render-snapshot --base-url https://www.leeserie.com` → `[OK] 26 route(s) captured`, after which all nine build-tree gates returned real verdicts on a client with no static export. |
| B-025 | 2026-08-08 | 2026-08-10 | `pipeline/audit/remediate.py:350-352` (`already_fixed`) + `plan.py` generally | **A `no_change` for a structural reason is retried on every future run, at full cost, forever.** `already_fixed` collects only `{fp for i in changelog["items"] if i.get("status") == "fixed"}`, so `selectable()` re-queues every `no_change` item next run. That is right for a transient refusal (a timeout, a confused agent) and wrong for a permanent one. On `lee-series-web` the first live `thin_content` run produced four `no_change`s with an identical, correct, and **permanent** reason: the PDP body copy (`description`, `benefits`, `instruction`, `faqs` — the bulk of the word count) is fetched at request time from **Firestore** via `lib/catalog.ts`'s `getProductSetBySlug`. It is not in the repository at any path, so **no tier can fix it** — not T1, not T2, not T3. Nine of lee's fifteen `thin_content` items are these Firestore-backed PDPs. **The artifacts this entry was measured from were deleted on 2026-08-08** at the operator's instruction (local checkout discarded before a re-onboard; `lee-wave/lee-series-web` on GitHub never carried them — `docs/audit/` was untracked). The counts and the refusal reason above are quoted from the run output at the time and are **not re-runnable**; treat them as a recorded observation, not a live reproduction. Re-onboarding lee will re-measure and should reproduce the same nine. Each future `wf-site-remediate` run will hand all nine to Claude Code again, pay for nine investigations, and get nine identical refusals; each future `wf-site-plan` will re-file them as PERSISTING. The agent behaved perfectly — §3 of the skill says change nothing and say `NO CHANGE` with the reason, and it did, naming the exact file and the exact allow-list it was measured against. The engine is what has no memory of it. | **Fixed 2026-08-10** by `wf-site-remediate --recommend`, which turns the wasted spend into a deliverable rather than suppressing it. Same loop, same before/after `git status` measurement, **opposite assertion**: the tree must come back clean and a run that modified a file is refused and stopped like an out-of-tier edit. Briefs land in `docs/audit/human-worklist.md` — deliberately NOT under `docs/audit/<cycle>/`, because a page whose copy lives in a CMS is a fact about the site's architecture and filing it per-cycle would let next cycle's empty folder re-queue all nine. The brief file IS the skip list: `selectable()` excludes any fingerprint carrying a brief, so there is no `unfixable:` config block and no hand-maintained fingerprints, and the agent cannot suppress its own work because writing a brief only happens in a mode the operator invokes and that mode must finish clean. `plan.py` stamps `human_edit` and gives them a *Briefed for a Human Editor* heading, so the money leak was not traded for a blind spot. **Part (2) of the original entry — a finding state meaning "real, but not fixable from here" — is what `human_edit` now is;** B-022 remains its own separate decision. Ran live on `/product/rice-cake-cleanser/` (`words=442`): `[BRIEFED] wi-2026-08-0101 ... [OK] 1 brief(s) written, $0.6464`, `git status` clean afterwards, and the brief emitted two `[NEEDS FROM CLIENT: ...]` blocks rather than inventing the copy. `tests/test_recommend_mode.py`, 8 tests. `.venv/bin/pytest -q` → `655 passed in 5.80s`. |
| B-024 | 2026-08-08 | 2026-08-08 | `pipeline/audit/plan.py:66` | **`thin_content` was mapped to `min_tier: 2`, so every thin page on a T1 client was filed unactionable — including the ones the client's own `text_paths` already permitted fixing.** The tier map keys off the *finding kind*, on the assumption that thin content means "write a new page". `measure.py` only ever measures **live URLs**, so a page cannot be measured as thin unless it already exists; the fix is always "expand the copy that is there", which is a modify. On `lee-series-web` this blocked **15 of 114** work items — `/learn/*` (copy in `lib/learn-guides.ts`), `/product/*` (`lib/seo-pdp-copy.ts`), `/app/`, `/contact-us/` (`app/(site)/**/page.tsx`) — and **all three of those paths are already in lee's `text_paths`**, so a T1 agent was permitted to edit every one of them and was told not to try. Measured 336–496 words against `min_words: 500`. Worse on a data-driven repo: raising lee to T2 would not have helped, because T2 grants *creates* under `content.location` and lee has nowhere to create — `LEARN_GUIDES` is a typed array in a single 180-line file with a union-typed slug (`lib/learn-guides.ts:6`) behind a dynamic `app/(site)/learn/[slug]/page.tsx` route. Declaring a `content.location` to unblock this would have been the "authority over nowhere" failure `CLAUDE.md` names. | **Fixed 2026-08-08**, `min_tier` 2 → 1, with the reasoning in a comment at the call site so it does not get "corrected" back. The safety did not move: `tier_check` still judges the real diff, so a client whose thin page's copy is *not* in `text_paths` is still refused — at the diff, which is where the tier model puts that judgement. Regression test `tests/test_plan.py::test_thin_content_is_actionable_at_t1`; the pre-existing `test_tier1_blocks_content_work_but_keeps_it_visible` used `thin_content` as its T2 example and was rewritten around `health.h1_count` (T3), which is structural and stays blocked. `.venv/bin/pytest -q` → `621 passed in 5.31s`. **Note this does not touch `schema_faq_missing`, the other `min_tier: 2` entry** — that one is B-022 and is a separate decision. |
| B-023 | 2026-08-08 | 2026-08-08 | `docs/gate-reference.md` §"The gates" | **Three gates deleted a release ago were still documented as BLOCKING, with green verification results.** `pages-are-data-check.py`, `brief-fanout-check.py` and `validate_multistate_config.py` all went in `79b0b5b` ("Remove DOCX intake, the emitter, and emitter-bound gates") — `brief-fanout-check` reads `docs/briefs/*.json`, a `pipeline/generate` artifact that no longer exists. Their rows survived unchanged, each reading **BLOCKING** with a **WORKS** status and an exit code in the registry column. The doc contradicted itself: line 103 already noted `pages-are-data-check` "went with the emitter in v3 §3 and its entry was dead", while line 144 still listed it as a live blocking gate. An operator reading the canonical gate doc would count 22 gates against the 19 that exist, and would look for three files that are not there. This is the exact failure mode `CLAUDE.md` §3 names. Found while correcting gate claims in the new `page-type-shapes.md` reference, by listing every gate filename in the doc and testing each against `pipeline/gates/`. | **Fixed 2026-08-08**, same commit. Rows marked `**REMOVED**` and struck through, each naming `79b0b5b`, rather than deleted — the table carries an "observed Run #1" column, so it is partly a verification record and deleting rows would destroy evidence of what was checked. `MODULES.md` was already correct at 19. Line 184's "19 of 21 gates" is a Run #1 statement and was left as history. Verification: `for g in $(grep -oE '^\| ~?~?`[a-z_-]+\.(py\|sh)' docs/gate-reference.md ...); do test -f pipeline/gates/$g \|\| pipeline/deploy/$g; done` → the only remaining misses are the three deploy scripts, which live in `pipeline/deploy/`, and the three now marked REMOVED. |
| B-008 | 2026-08-06 | 2026-08-07 | `pipeline/gates/em_dash_check.py` | **A legacy site with em dashes in its existing copy is permanently red.** `em_dash_check` is in neither `BASELINEABLE` nor `NEVER_BASELINEABLE` — it takes no `--baseline` at all, so B-007's fix cannot reach it. Point the suite at a client site whose pre-pipeline copy contains a single em dash and the gate blocks every PR forever, with no recording that can accept it and no documented refusal explaining why it must not be accepted. `robots_aicrawler_check` and `client_docs_check` are in the same third category, but both are binary conditions a one-time fix clears; em dashes are unbounded legacy prose. | Not fixed. Adding a gate to `BASELINEABLE` is a documented human decision, not something to do mid-investigation — `pipeline/lib/baseline.py` says so at the set definition, and the argument against is that an em dash is a house writing standard we are paid to enforce, not inherited debt. The alternative is to fix the client's em dashes once at onboarding, which is a real answer. Needs Alex's call. | Made baselineable, which is the decision it was waiting for: a legacy em dash is content debt of the same class as a heading that is not in Title Case, and `check_headings` was already in `BASELINEABLE`. `NEVER_BASELINEABLE` is for live falsehoods and structural invariants; this is neither. Three parts, because the registry entry alone does nothing (B-007): the gate emits `Finding`s instead of printing tuples, `gate_argv` learned how to invoke it, and the workflow passes `steps.baseline.outputs.arg` — that last one was caught by `test_every_baselineable_gate_receives_the_baseline`, which failed the moment the gate joined the set. Fingerprint is the offending TEXT, so a line moving does not re-block the PR. Proven on a real crawl: `wf-gate-baseline` recorded 2 entries, then `wf-em-dash-check --baseline` -> `PASS: no new em dashes (2 pre-existing accepted as legacy debt)` exit 0; a third em dash written into new copy -> `FAIL: 1 NEW em dash(es)` exit 1. `tests/test_ratchet_wiring.py::test_the_em_dash_ratchet_accepts_legacy_and_blocks_new`, `::test_a_line_moving_does_not_turn_legacy_debt_into_a_new_finding`, `::test_the_recorder_knows_how_to_invoke_em_dash_check`. |
| B-016 | 2026-08-07 | 2026-08-07 | `pipeline/gates/claim_provenance_check.py` `prose_from` + `pipeline/audit/plan.py:219` | **Every client's first cycle PR is refused, by the engine's own boilerplate.** `.md` is in `TEXT_SUFFIXES`, so the gate treats every changed markdown file in the diff as human-facing prose end to end — including `docs/audit/<cycle>/report.md`, which `wf-site-plan` generates. Line 219 of `plan.py` writes `- Compared Against: {prior_cycle or 'nothing — this is the first cycle'}`, and `SUPERLATIVE_RE` catches `first`, which appears in no config field and no work-item evidence. Result: **exit 18 REFUSED** on a sentence the pipeline wrote about itself. Hit on the real `lee-series-web` 2026-08 PR: `[UNSOURCED] docs/audit/2026-08/report.md: 'first' (superlative)` with `tier_check` green over the same six paths. It is not client copy, never renders on the site, and `tier_check` already classifies the identical file as a `cycle artifact` — so the two gates disagree about what `docs/audit/**` is. Reproduces on every client whose cycle has no prior to ratchet against, i.e. every first PR. | Candidate (a): `prose_from` skips `ARTIFACT_PATHS` **imported from `lib/common`** — the same list `tier_verdict` calls a `cycle artifact`, so the two gates cannot drift about the same paths. Real gate on a scratch repo carrying the exact refusing line: `[OK] claim-provenance: every claim in 1 changed file(s) resolves to a source.` exit **0**. Negative control, one page of real copy added to the same repo: `[UNSOURCED] src/content/about.mdx: '4.2 star' (rating)`, `'first' (superlative)`, `[BLOCKED] 2 unsourced claim(s).` exit **18** — narrowed, not disarmed. `tests/test_phase4_gates.py::test_the_cycle_report_is_not_scanned_as_client_prose`, `::test_the_two_gates_now_agree_about_the_same_path`, `::test_client_markdown_outside_the_audit_dir_is_still_scanned`. |
| B-015 | 2026-08-07 | 2026-08-07 | `pipeline/dashboard/server.py` `COMMANDS` + `static/page-runs.js` | **The console's local gates report green over work they never looked at.** `tier-check` and `claim-provenance` are offered on the Runs screen as pre-PR checks, and the flow everyone reaches for is remediate → check → commit. But both gates diff `origin/main...HEAD` (`tier_check.py:104`, `claim_provenance_check.py`) — the **three-dot** form, which compares commits and cannot see the working tree. Run either on a dirty checkout with no cycle commit and the diff is empty, every gate passes vacuously, and the console prints `Clean — every check passed`. That is the exact failure the exit vocabulary exists to prevent: a run that judged nothing rendering identically to a run that judged everything and approved it. Hit on lee-series-web while shipping 2026-08 — the fix was to reorder the operator's steps to commit-then-gate, which nothing in the UI or the docs says. | Both gates carry `needs_commit`; `_start_run` refuses with **409** and the reason when `commits_to_judge()` is 0. The gates are untouched — `--base HEAD` would diverge from what CI runs. "Cannot tell" (no remote-tracking ref) lets the gate run and speak for itself, so a local-only checkout is not refused. `tests/test_dashboard.py::test_an_uncommitted_tree_has_nothing_for_those_gates_to_judge`, `::test_a_cycle_commit_gives_those_gates_something_to_judge`, `::test_no_remote_ref_is_cannot_tell_not_nothing_to_judge`, `::test_only_the_three_dot_gates_carry_needs_commit`. |
| B-014 | 2026-08-07 | 2026-08-07 | `pipeline/audit/scaffold_client_docs.py` + `pipeline/gates/tier_check.py` | **Everything `wf-onboard` scaffolds is un-committable on a cycle branch, and nothing says so.** `wf-scaffold-client-docs` writes `docs/INDEX.md`, `docs/seo-progress.md`, `docs/seo-work-log.md`, `docs/cycle-logs/.gitkeep`, `docs/intake-archive/.gitkeep`, and `wf-bootstrap-config` writes `docs/client-config.yml`. On a T1 client all six are **creates**: the five docs match no `text_paths` glob, and `client-config.yml` is on the deny floor, refused at every tier including T3. The dashboard's only staging action is `git add -A` (correct, per B-011), so the documented dashboard sequence — branch → stage → commit → push → PR — sweeps all six into the cycle PR and `tier_check` refuses it with **exit 17**. Verified against `lee-series-web` by running `tier_verdict` over its real `git status -uall`: 4 audit artifacts OK ("cycle artifact"), 6 refused. Neither `HOW-IT-WORKS.md` nor the console mentions that the scaffold has to land on `main` in a separate human commit first. | `onboard.commit_scaffold` commits the six paths to the DEFAULT branch by named pathspec, never `git add -A`, refusing off the default branch, refusing when a scaffold path is tracked-and-modified (that is the operator's interview edit), and never pushing. Candidate (a) from the ledger, with the constraints that answer the 'new class of surprise' objection. Found while testing: `_git_out().strip()` ate the leading space of ` M path`, so an unstaged human edit read as staged — `_git_status` reads porcelain unstripped. `tests/test_onboard.py::test_the_scaffold_commit_takes_nothing_it_did_not_write`, `::test_the_scaffold_is_never_committed_on_a_cycle_branch`, `::test_a_humans_edit_to_a_scaffold_path_is_not_committed_for_them`, `::test_a_second_onboarding_does_not_fail_on_the_first_ones_commit`. |
| B-013 | 2026-08-07 | 2026-08-07 | `pipeline/audit/remediate.py` `selectable` + `main` | **A capped run does not resume, and the second run destroys the first one's record.** The module docstring states that on hitting `--max-items` "the remaining items keep their place in the worklist for the next run" (`remediate.py:40`), and `CLAUDE.md` repeats it as "`wf-site-remediate` leaves un-worked items in the worklist". Neither is what the code does: `selectable()` rebuilds the queue from `worklist.json` sorted by `(lane, id)` and never reads `changelog.json` (`remediate.py:308-317`), so run two starts at item 1 again and re-attempts the same first N. Meanwhile `main` writes `changelog.json` wholesale (`remediate.py:442-443`), so run two's record replaces run one's — the reviewed evidence for the items that were actually fixed is gone. Consequence: the documented "run 10, then run 10 more" workflow silently does the same 10 twice and clears the audit trail; the only way through a worklist is a single run with the cap raised. Found while explaining the `--max-items` default to the operator, then read against the code. | `selectable()` skips items the cycle changelog records as `fixed`; `main` MERGES that changelog (prior entries survive, a fresh attempt replaces its own earlier entry, `cost_usd`/`runs` accumulate, a `--dry-run` merges nothing). The changelog decides what is ATTEMPTED, never what is VERIFIED — `acceptance_check` still re-measures. `_base` carries `finding_fp`. `tests/test_remediate.py::test_a_rerun_skips_what_the_changelog_records_as_fixed`, `::test_the_second_run_merges_rather_than_destroying_the_first`, `::test_a_dry_run_never_touches_the_record_of_runs_that_wrote`, `::test_a_status_other_than_fixed_is_retried`, `::test_the_changelog_carries_the_fingerprint_of_the_finding_it_fixed`. |
| B-010 | 2026-08-06 | 2026-08-07 | `pipeline/audit/bootstrap_config.py` + `pipeline/audit/preflight.py` | **A freshly bootstrapped client can never reach the interview step.** `preflight` requires a top-level `industry` (`preflight.py:24`); nothing in `pipeline/` ever writes it — the generated config carries `business.trade` instead. So the very first `wf-onboard` on a new client always stops at exit **11** ("missing required fields"), never the documented exit 12 ("has TODOs, this is the interview"). Reproduced on the first onboarding of `lee-wave/lee-series-web`: `[STOP] Config missing fields: ['industry']`, `wf-onboard` exit 1, before a single TODO was read. | `wf-preflight` on a fresh bootstrap: `[STOP] Config has unresolved TODOs: ['.business.trade']` exit **12** — the documented interview step, not exit 11. `industry` dropped from `preflight.required`; the summary line reads `business.trade`. One fact, one place. |
|---|---|---|---|---|---|
| B-012 | 2026-08-07 | 2026-08-07 | `pipeline/dashboard/server.py` `_launch` | **The dashboard would start a second agent inside a checkout the first was still editing.** `_launch` built a `Run` unconditionally — no check for a live run on the same client — so a second click of RUN (or a second browser tab) started a second `wf-site-remediate` against the same repo. Both processes run Claude Code with Write/Edit over the same files; the loser's edits are silently overwritten, and `changelog.json` is written wholesale at the end of each run, so the second finisher's version is the only record that survives. Observed on `lee-series-web`: two remediate runs at 08:40 and 08:58, the 08:40 one producing 706KB of agent output then dying with no `changelog.json` written, leaving `docs/audit/2026-08/changelog.json` still showing the previous day's all-errored run. Found while confirming an operator's flow, not by a test. | `busy_run(slug)` returns the run for that client whose `exit_code is None`; `_launch` refuses it with HTTP **409** naming the command still going. Check-and-insert happen under `RUNS_LOCK` because `ThreadingHTTPServer` serves concurrent POSTs and a bare check-then-insert lets both callers pass. Keyed on slug rather than cwd — onboard's cwd is the shared clients dir, so a cwd key would let two onboards block each other while still permitting the real conflict. **Known scope:** `RUNS` is per-process, so a `./run.sh wf-site-remediate` in a terminal is not covered; a lockfile in `remediate.py` is the upgrade if that path ever bites. | `tests/test_dashboard.py::test_a_live_run_makes_that_client_busy` (a run with `exit_code is None` makes `busy_run` return it), `::test_a_finished_run_does_not_block_the_next_one` (exit 0 → `None`), `::test_another_clients_run_does_not_block` (the guard is per checkout, not fleet-wide), `::test_no_run_for_this_client_is_not_busy`. Full suite `.venv/bin/python -m pytest -q` → `419 passed in 2.65s`. |
| B-011 | 2026-08-07 | 2026-08-07 | `pipeline/dashboard/server.py` `GIT_ACTIONS` + `static/page-git.js` | **The dashboard could only produce a hollow PR.** The only staging action was `stage-audit` → `git add docs/audit`, and `commit` → `git commit -m <msg>` with no `-a` and no pathspec, so it commits *only what is staged*. `wf-site-remediate` writes two kinds of change — the audit JSON **and** the site files Claude Code edited — so an operator who did branch → stage → commit → push → PR entirely in the dashboard opened a PR containing `changelog.json` claiming N fixes and **none of the fixes themselves**. The Git page made it visible but not fixable: `renderChanged` prints full `git status --porcelain`, so `M src/page.tsx` sat in the list with no button that could stage it. Found by reading the code while answering "can I commit and push everything in the dashboard", then reproduced. | `stage-audit` replaced by `stage-all` → `git add -A` (not a second button beside it: two near-identical staging buttons is the same footgun with a longer name). Safe because staging is not the last word — `tier_check` judges the whole PR diff, so an out-of-tier file staged here fails the gate rather than reaching production, and `wf-site-remediate` itself already exits 9 REFUSED before that. | Reproduced in a scratch repo — one edited `src/page.tsx` plus one `docs/audit/2026-08/changelog.json`, then the old sequence: `git add docs/audit && git commit -m "audit: acme cycle artifacts"` → `git show --name-only` prints **`docs/audit/2026-08/changelog.json`** alone, and `git status --porcelain` still shows **` M src/page.tsx`** left behind. Same repo with the fix: `git add -A && git commit` → `git show --name-only` prints `src/page.tsx`, `git status --porcelain` empty. `tests/test_dashboard.py::test_staging_covers_the_remediators_code_edits_not_just_the_audit_json` asserts `stage-all` → `["git","add","-A"]` and that `stage-audit` is now an unknown action. Full suite `.venv/bin/python -m pytest -q` → `415 passed in 2.69s`. |
| B-009 | 2026-08-06 | 2026-08-06 | `pipeline/audit/measure.py:117` | **`health.img_alt_missing` fired on `alt=""`.** The test was `re.search(r'\salt="[^"]+"', img)` — a *present but empty* alt fails it, so every correctly-marked decorative image was reported as a defect. WCAG treats `alt=""` as the deliberate "decorative, skip me" signal. On the first live run against `www.leeserie.com` it produced **1158 of 1272 findings (91%)** and buried every real finding. Verified against the live homepage before touching the code: 76 `<img>`, 38 with real alt text, 38 with `alt=""`, and **zero with no alt attribute at all**. | The predicate is now `if not re.search(r'\salt=', img)` — only an ABSENT attribute is a defect. A second predicate for a *meaningful* image wrongly marked `alt=""` would need a heuristic we do not have; the old behaviour was not that heuristic, it flagged everything. Shipped alongside `warn_dominant_code`, which prints a `[WARN]` when any one code is ≥50% of a run — the property that would have made this visible in the run output instead of only in a hand count. | `tests/test_measure.py::test_decorative_alt_is_not_a_missing_alt` (one decorative, one described, one genuinely missing → exactly one finding), plus `::test_a_dominant_code_is_warned_about` and `::test_a_mixed_run_is_not_warned_about`. Full suite `413 passed in 2.59s`. Live re-measure of the same site: `[OK] 26 URLs measured, 114 findings` (was 1272), worklist `21 actionable, 93 above tier`. |
| B-007 | 2026-08-06 | 2026-08-06 | `.github/workflows/quality-gate.reusable.yml` | **The ratchet was implemented, tested, and called by nothing.** Not one of the **7** baselineable gates was invoked with `--baseline`, and `add_baseline_args` defaults it to `None` with no auto-discovery of `docs/gate-baseline.json`. Every gate ran bare and reported the client's *inherited* debt as blocking. `lib/baseline.py` and `wf-gate-baseline` worked and were tested; nothing called them on a PR. The docs described the fleet as running "green-on-legacy via the baseline" (`README.md`, `MODULES.md`, `gate-reference.md`); for a client repo it did not, so the first PR against any real site was red across the board — and the only ways out were "fix the whole site first" or "switch the gate off", which is exactly what the ratchet exists to prevent. Reproduce on the parent commit: `grep -n 'baseline' .github/workflows/quality-gate.reusable.yml` → one hit, a comment. (The ledger's first entry said 16 baseline-aware gates; the real number is 7 — `em_dash_check`, `robots_aicrawler_check` and `client_docs_check` take no baseline at all, which is now **B-008**.) | A `Resolve the gate baseline` step sets the flag from a file-existence test and each of the 7 gates takes `${{ steps.baseline.outputs.arg }}`. **Absent file → no flag → bare run**, i.e. the old behaviour, plus a `::warning::` naming the recording command; a `--baseline` pointing at a missing file stays a hard refusal, since a typo silently disarming the ratchet fleet-wide is the worse bug. Dead `pages_are_data_check` dropped from `BASELINEABLE` and `gate_argv` — deleted with the emitter, it would have failed as "produced no findings file" rather than a clean refusal. | `tests/test_ratchet_wiring.py` — 35 tests reading the workflow as text: every `BASELINEABLE` gate carries the flag, no `NEVER_BASELINEABLE` gate does, every named gate still exists on disk. Negative control run: removing the flag from one gate fails `test_every_baselineable_gate_receives_the_baseline[capsule_check]`, restoring it passes 35/35. Full suite `361 passed in 2.54s`. |
| B-006 | 2026-08-05 | 2026-08-05 | `pipeline/audit/remediate.py` `snapshot` | `git status --porcelain` collapses a new untracked directory to `src/content/` and never names the file inside it. A T2 page creation was therefore recorded — and judged by `tier_verdict` — as a *directory* that matches no allow-list, so every T2 authoring run would have been refused for writing exactly the file it was told to write. | `-uall` added. | `tests/test_remediate.py::test_t2_may_create_a_page_and_wire_the_registry` — asserts both the new `.mdx` and the registry file appear in the item's `files`. |
| B-005 | 2026-08-05 | 2026-08-05 | `pipeline/lib/common.py` `path_matches` | **Deny-floor bypass.** The path was normalized with `path.lstrip("./")`, which strips *characters*, not a prefix — so `.github/workflows/ci.yml` became `github/workflows/ci.yml` and matched no deny pattern. The agent could have edited the gates that judge it, at every tier, and `tier_check` would have reported the diff clean. Found by the first run of the gate's own test table. | Leading `./` removed as a prefix, then `/` alone stripped. `.` is never stripped. | `tests/test_phase4_gates.py::test_glob_matching[.github/**-…]` and `::test_t3_may_do_anything_except_what_is_denied`, which asserts all five deny patterns refuse at T3. |
| B-004 | 2026-08-05 | 2026-08-05 | `pipeline/audit/remediate.py` `op_for` | The create/modify verdict was inferred as "absent from the before-snapshot, present in the after-snapshot = a create". The snapshot only holds *dirty* paths, so a clean tracked file being edited is absent from it — meaning **every ordinary T1 edit was judged a creation and refused**, and T1 could never have fixed anything. | The operation now comes from git's own status letters (`??`/`A` → create, `D` → delete, else modify) instead of being inferred from presence. | `tests/test_remediate.py::test_an_in_tier_fix_is_recorded_against_the_file_it_actually_changed` (passes) alongside `::test_the_map_is_measured_not_claimed` (a genuine create is still refused at T1). |
| B-003 | 2026-08-05 | 2026-08-05 | `pipeline/audit/remediate.py` `run_agent` | The remediation prompt was passed as an argv positional to `claude -p`. The prompt opens with `SKILL.md`, whose first line is `---`, and the CLI's option parser read that as a malformed flag: `unknown option '---…'`. Every item would have failed with `status: "error"` and zero files changed. **No hermetic test could see it** — they all stub `run_agent` — so it survived 300 passing tests and was found by the first live run against a real model, exactly as B-001 was. | Prompt moved to stdin; the SKILL.md YAML frontmatter is also stripped before inlining, since it is loader metadata rather than instructions. | `tests/test_remediate.py::test_the_prompt_goes_on_stdin_not_argv` (stubs `subprocess.run` one level lower and asserts no argv element starts with `---`) and `::test_the_skill_frontmatter_is_not_sent_to_the_writer`. Live: `[FIXED] wi-2026-08-0001 health.desc_length on /roofing/ -> src/data/services.ts`, `$0.4054`, followed by tier-check / provenance / acceptance all exit 0. |
| B-002 | 2026-08-05 | 2026-08-05 | `pipeline/audit/bootstrap_config.py` | The last of the three config template blocks was missing its `f` prefix, so **every config `wf-bootstrap-config` has ever generated is unloadable YAML**: it emitted `framework: {framework}` verbatim and `per_service: {{}}`, which PyYAML refuses with `found unhashable key`. Every downstream `load_config` on a freshly bootstrapped repo died at line 108 — and the four `repo:` keys the build action reads (`framework`, `sitemap`, `pages_dir`, …) were placeholder text, not values. Found while verifying phase 2's tier block end to end. | `f` prefix restored (its `{{}}` escapes were already written for one); duplicate `required_phrases` / `schema_type` / `faq_seed_questions` keys removed from the same block. `main()` now parses the config it built and exits 3 rather than writing YAML nothing can load — a generator that emits a broken file must refuse, not defer the failure five commands downstream. | `tests/test_tiering.py::test_bootstrapped_config_is_loadable_yaml_carrying_the_tier` — bootstraps a fixture repo, then asserts `yaml.safe_load` succeeds, `repo.framework == "nextjs-app-router"` and `h1_format.per_service == {}`. |
| B-001 | 2026-08-05 | 2026-08-05 | `pipeline/lib/common.py` `curl` / `curl_status` | A hung host raised `subprocess.TimeoutExpired` uncaught, crashing the run with a traceback 30s in. In `wf-site-health` this bypassed the exit-19 refusal path entirely, so a total outage exited 1 with a stack trace instead of a clean REFUSED. Latent in `audit_live.py` since import, and shared by all four callers (`measure`, `poll_live`, `bootstrap_config`, `preflight`). | Guard added at both helpers, returning their existing failure signals (`""` / `0`). Fixed at the shared function, not per caller. | `tests/test_common.py::test_curl_returns_empty_on_timeout`, `::test_curl_status_returns_zero_on_timeout`. Smoke run against an unresolvable domain now prints `[REFUSED] … nothing to measure` and `exit=19`, writing no `findings.json`. Found by that same smoke run — no hermetic test could have caught it, since they all monkeypatch `curl`. |
